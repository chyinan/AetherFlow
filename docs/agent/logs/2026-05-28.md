# 2026-05-28 Agent Log

## GW-AI-PROVIDER-ROUTE-20260528 主线合入记录

时间：2026-05-28 13:05:00 +08:00
状态：REVIEW

记录：
1. 远端 main 已 fast-forward 到 `151605c`，包含 Gateway 路由提交和任务 handoff 文档。
2. feature/GW-AI-PROVIDER-ROUTE-20260528-gateway-route 与 main 已保持一致。
3. 本地 `git fetch origin main` 因 GitHub HTTPS 连接 reset/超时失败；远端主线状态已通过 GitHub API 校验。
4. 统一运行电脑验证仍未执行，任务保持 REVIEW，不标记 DONE。
## GW-AI-PROVIDER-ROUTE-20260528 追加记录

时间：2026-05-28 12:30:12 +08:00
状态：REVIEW

记录：
1. 已为 ai-service 新增显式 Gateway 路由 `/ai/provider/**`，uri 为 `lb://ai-service`，order 为 `-50`。
2. 已保持现有 `/ai/**` 泛路由不变，同时补充 `ai-provider-management` 的 Sentinel IP 限流规则，避免新路由绕过 route-id 维度治理。
3. 已补充路由测试，验证 `/ai/provider/status`、`/ai/provider/policy`、`/ai/provider/metrics` 和 `/ai/provider/policy/recover/openai` 会优先匹配到 ai-provider-management 路由。
4. docs-only claim 已提交：`d3c7e10c00cef82a8be63bcb70f3a4bd4da2c539 docs(agent): claim GW-AI-PROVIDER-ROUTE-20260528`。
5. 由于 git HTTPS 连接 github.com 不稳定，远端分支通过 GitHub API 更新成功。
6. 业务代码已提交：`7e2cb3be5d1668d230a6eceae66ddfb62b57beb4 feat(gateway): add ai provider route`。
7. `JAVA_HOME` 已显式切到 `C:\Program Files\Microsoft\jdk-17.0.19.10-hotspot` 执行 Maven 验证。
8. `mvn -pl backend/gateway-service -am test` 已通过，common 8 tests、gateway-service 16 tests、BUILD SUCCESS。
9. `git diff --name-only main...HEAD` 已执行，变更文件限定为 AGENT.md、gateway-service application.yml、GatewayRouteConfigurationTest.java 和本任务 docs。

文件锁状态：
1. backend/gateway-service/**：RELEASED
2. docs/agent/tasks/GW-AI-PROVIDER-ROUTE-20260528.md：RELEASED
3. docs/agent/logs/2026-05-28.md：RELEASED
4. AGENT.md：RELEASED

交接：
任务已完成 Gateway 接入与测试，状态建议保持 REVIEW，等待负责人合入 main。
## GW-AI-PROVIDER-ROUTE-20260528

时间：2026-05-28 12:19:24 +08:00
Agent ID：chyinan
Session ID：SESSION-20260528-1219-codex-gw-ai-provider-route
分支：feature/GW-AI-PROVIDER-ROUTE-20260528-gateway-route
状态：IN_PROGRESS

记录：
1. 已读取 AGENT.md 和 docs/COMMON_CONTRACTS.md。
2. 已确认任务边界完整：本任务只负责 gateway-service 路由接入，不修改 ai-service Provider Orchestration 业务逻辑。
3. 已检查 gateway-service 现有 application.yml 路由、JWT 白名单、Sentinel 配置和 GatewayRouteConfigurationTest。
4. 发现主线已有 /ai/** 泛路由，本任务将新增显式 /ai/provider/** 路由并用测试锁定 status/policy/metrics 的匹配。
5. 原工作区当前在 feature/AI-PROVIDER-ORCH-20260528-provider-orchestration，存在 frontend 未提交改动；为避免触碰无关改动，已从 local main 创建独立 worktree D:\Programs\AetherFlow-gw-provider-route。
6. git fetch origin main 连续失败，原因是 GitHub HTTPS 连接超时或 reset；本地 main 与 origin/main 均为 62aa707，已从 local main 创建本任务分支。
7. 当前进行 docs-only claim；claim push 成功前不修改 backend/gateway-service 业务代码。

文件锁申请：
1. backend/gateway-service/**
2. docs/agent/tasks/GW-AI-PROVIDER-ROUTE-20260528.md
3. docs/agent/logs/2026-05-28.md
4. AGENT.md

验证计划：
1. git diff --name-only main...HEAD
2. 使用 JDK 17 运行 mvn -pl backend/gateway-service -am test

当前风险：
1. /ai/provider/** 与现有 /ai/** 泛路由重叠，需要显式 route order 或顺序保证 provider 管理路径优先匹配。
2. 新增 provider route-id 后，需要按现有 Sentinel route-id/IP 限流风格补治理规则，避免新增 route 绕过限流。
3. 默认 java/mvn 指向 Java 11，Maven 验证需显式使用 C:\Program Files\Microsoft\jdk-17.0.19.10-hotspot。
4. GitHub HTTPS 网络不稳定；若 claim push 失败，任务不能进入编码阶段。
## AI-PROVIDER-ORCH-20260528

时间：2026-05-28 11:40:07 +08:00
Agent ID：chyinan
Session ID：SESSION-20260528-1140-codex-ai-provider-orch
分支：feature/AI-PROVIDER-ORCH-20260528-provider-orchestration
状态：REVIEW

记录：
1. 已调用 TDD skill 工作流，采用行为测试优先的 tracer bullet 方式实现 Provider Orchestration。
2. 已读取 AGENT.md、docs/COMMON_CONTRACTS.md 和 backend/ai-service 现有 Provider/Router/Controller/Sentinel/Cache/Test 代码。
3. 已确认当前 main 工作区干净；git pull origin main 首次失败后重试成功。
4. 已创建任务分支 feature/AI-PROVIDER-ORCH-20260528-provider-orchestration。
5. 当前进行 docs-only claim；claim push 成功前不修改 backend/ai-service 业务代码。

文件锁申请：
1. backend/ai-service/**
2. docs/agent/tasks/AI-PROVIDER-ORCH-20260528.md
3. docs/agent/logs/2026-05-28.md
4. AGENT.md

验证计划：
1. git diff --name-only main...HEAD
2. mvn -pl backend/ai-service -am test

当前风险：
1. 本机 Java 为 11，不符合项目 Java 17 要求；如不能切换 JDK 17，则 Maven 验证需要统一运行电脑补测。
2. 新增 /ai/provider/** API 属 ai-service 自有管理 API，不修改 Gateway；前端网关接入需后续单独任务。

实施记录：
1. 用户提供 JDK 17 路径 C:\Program Files\Microsoft\jdk-17.0.19.10-hotspot；Maven 验证已使用 Java 17.0.19。
2. 已升级 AiProvider 抽象，新增 generate/stream/embedding/health，保留 complete 兼容现有节点执行器。
3. 已新增 Redis Provider Governance：Routing Policy、Circuit State、Health、Active Provider、Inference Logs、Metrics。
4. 已重写 AiProviderRouter，支持 priority routing、retry、failover、circuit open skip、half-open probe lock、active provider 写入和观测日志。
5. 已新增 Provider Status/Policy/Metrics API，并补手动恢复入口 /ai/provider/policy/recover/{provider}。
6. 已扩展 Sentinel 资源保护：ai-provider-router、ai-provider-openai、ai-provider-ollama、provider health/status/policy/metrics。
7. 已更新 backend/ai-service/src/main/resources/application.yml，暴露 provider priority、failover、auto recover、retry、circuit、health check 和日志窗口配置。

验证结果：
1. mvn -pl backend/ai-service -am test：通过；common 8 个测试通过，ai-service 16 个测试通过，BUILD SUCCESS。
2. 新增 router 行为测试覆盖 priority failover、open circuit skip、provider retry、threshold open、explicit provider failover。
3. 新增 controller 单元测试覆盖 /ai/provider/status、/ai/provider/policy update、/ai/provider/metrics。

交接：
任务ID：AI-PROVIDER-ORCH-20260528
完成内容：企业级 AI Provider Orchestration System 已实现，包含 Provider 抽象、Router、Policy、Redis 状态、熔断、Failover、Health Check、日志、Metrics、Status/Policy API、Sentinel 资源保护和 Retry。
修改文件：backend/ai-service/**、docs/agent/tasks/AI-PROVIDER-ORCH-20260528.md、docs/agent/logs/2026-05-28.md、AGENT.md
测试结果：mvn -pl backend/ai-service -am test 通过。
PR/提交/分支：feature/AI-PROVIDER-ORCH-20260528-provider-orchestration；claim ee316ab；业务提交 67b2e73。
合入 main：未合入
统一运行电脑验证：未运行
遗留问题：前端若经 Gateway 访问 /ai/provider/** 需另开 Gateway 路由任务；真实 OpenAI/Ollama 需要统一运行电脑联调。
下一步：负责人 Review 后合入 main。
文件锁：RELEASED

## FILE-SERVICE-GOVERNANCE-20260528

时间：2026-05-28 16:10:00 +08:00
Agent ID：chyinan
Session ID：SESSION-20260528-FILE-SERVICE-GOVERNANCE-INTEGRATION-CODEX
分支：feature/FILE-SERVICE-GOVERNANCE-main-integration
状态：IN_PROGRESS

记录：
1. 用户要求按审阅结论处理 feature/FILE-SERVICE-GOVERNANCE-enterprise-review 合入主线问题。
2. 已确认直接合 main 会在 backend/file-service/src/main/java/com/aetherflow/file/config/MinioConfig.java 和 backend/file-service/src/main/java/com/aetherflow/file/controller/InternalFileController.java 冲突。
3. 合并策略：从最新 main 开新分支，只移植 file-service 治理能力；MinioConfig 同时保留 FileInternalProperties 和 FileUploadProperties；InternalFileController 同时保留内部 token 校验和 Swagger 文档增强。
4. 额外安全修复：上传进度查询必须校验 X-User-Id 与进度记录 userId 匹配，避免通过 taskId 读取他人上传状态。
5. git fetch origin --prune 首次失败：GitHub 连接被重置；当前基于本地 origin/main=6469d1e 开工。

文件锁申请：
1. backend/file-service/**
2. docs/agent/tasks/FILE-SERVICE-GOVERNANCE-20260528.md
3. docs/agent/logs/2026-05-28.md
4. AGENT.md

验证计划：
1. git diff --name-only main...HEAD
2. git diff --check
3. mvn -pl backend/file-service -am test
4. 统一运行电脑补测 MySQL/Redis/MinIO/Nacos、Swagger、上传/下载/删除/进度/metrics/status。

实施记录：
1. docs-only claim 已通过 SSH 推送到 GitHub：8ecd37a docs(agent): claim FILE-SERVICE-GOVERNANCE-20260528。
2. 已按 file-service 文件边界移植治理能力，未直接 merge 旧治理分支，避免回退 main 上 AI/Gateway/前端/性能测试等已合入成果。
3. 已解决 MinioConfig 冲突：同时注册 FileInternalProperties 和 FileUploadProperties。
4. 已解决 InternalFileController 冲突：同时保留 X-Internal-File-Token 校验和 Swagger 文档示例。
5. 已补充上传进度访问边界：GET /files/progress/{taskId} 需要 X-User-Id；Redis 进度记录 userId 不匹配时返回 FORBIDDEN。
6. 业务代码提交已完成：cc7dbdd feat(file): integrate governance on main。

验证结果：
1. 默认 Maven 使用 Java 11，首次 mvn -pl backend/file-service -am test 因 Java 版本不匹配失败，未作为通过验证。
2. 设置 JAVA_HOME=C:\Program Files\Microsoft\jdk-17.0.19.10-hotspot 后，mvn -pl backend/file-service -am test 通过；common 8 tests，file-service 20 tests，BUILD SUCCESS。
3. git diff --cached --check 通过，无 whitespace error。

交接：
任务ID：FILE-SERVICE-GOVERNANCE-20260528
完成内容：file-service 治理能力已基于最新 main 集成，保留内部 token 安全校验，补齐进度查询用户隔离。
修改文件：backend/file-service/**、AGENT.md、docs/agent/tasks/FILE-SERVICE-GOVERNANCE-20260528.md、docs/agent/logs/2026-05-28.md
测试结果：mvn -pl backend/file-service -am test 通过；git diff --cached --check 通过。
PR/提交/分支：feature/FILE-SERVICE-GOVERNANCE-main-integration；8ecd37a；cc7dbdd。
合入 main：未合入
统一运行电脑验证：未运行，需补测 Redis/MinIO/MySQL/Nacos 以及上传/下载/删除/进度/status/metrics。
遗留问题：运行环境需确认 Redis 可用；旧 compare 分支不建议直接 merge。
下一步：推送 handoff 后由负责人 review 并合入 main。
文件锁：RELEASED

## AUTH-SERVICE-INIT

时间：2026-05-28 09:44:31 +08:00
Agent ID：codex-auth-20260528-001
Session ID：SESSION-20260528-AUTH-SERVICE-INIT-CODEX
分支：feature/AUTH-SERVICE-INIT-auth-service-basic
状态：IN_PROGRESS

记录：
1. 已读取 AGENT.md、docs/COMMON_CONTRACTS.md、docs/PROJECT_STRUCTURE.md 与 auth-service 现有骨架。
2. 已确认当前仓库为 main 分支的干净 checkout，并完成 git pull origin main。
3. 已创建 feature/AUTH-SERVICE-INIT-auth-service-basic 分支。
4. 已安装 JDK 17.0.19.10 到 C:\Users\25611\.codex\jdks\microsoft-jdk-17.0.19\jdk-17.0.19+10，用于后续 Maven 验证。
5. 当前先进行 docs-only claim，随后再开始 auth-service 业务代码修改。
6. docs-only claim 已本地提交：ddf99f5 docs(agent): claim AUTH-SERVICE-INIT。
7. git push -u origin feature/AUTH-SERVICE-INIT-auth-service-basic 失败：github.com HTTPS 连接被重置或超时。
8. 已检查 GitHub CLI 登录状态：当前账号为 xilingjie，token 可访问 GitHub API。
9. 已检查仓库权限：xilingjie 对 chyinan/AetherFlow 仅有 pull 权限，没有 push 权限。
10. 已尝试 SSH 路径，github.com:22 可连接，但当前账号无法添加 SSH key，且仓库 deploy key API 返回 404。
11. 任务按 AGENT.md 标记为 BLOCKED；claim push 成功前不修改 auth-service 业务代码。
12. 后续 GitHub 仓库权限恢复为 push=true。
13. feature/AUTH-SERVICE-INIT-auth-service-basic 已成功推送到 origin。
14. 任务状态已恢复为 IN_PROGRESS，继续实现 auth-service。

文件锁申请：
1. docs/agent/tasks/AUTH-SERVICE-INIT.md
2. docs/agent/logs/2026-05-28.md

验证计划：
1. git diff --name-only main...HEAD
2. git push -u origin feature/AUTH-SERVICE-INIT-auth-service-basic

当前阻塞：
1. 已解除。

## AUTH-SERVICE-INIT 追加记录

时间：2026-05-28 10:49:34 +08:00
状态：REVIEW

记录：1. 已完成 auth-service 的用户注册、登录、JWT 签发、当前用户查询和 /health 接入。
2. 已补充 Redis 依赖与配置，Swagger/OpenAPI 可通过 /v3/api-docs 和 /swagger-ui/index.html 访问。
3. 本地 JDK 17 运行 Maven test/package 通过，jar 烟测通过。
4. 统一运行环境 192.168.101.68 尚未执行联调验证，待负责人拉取分支后补测 Nacos/MySQL/Redis 实际连接。

验证：1. mvn -pl backend/auth-service -am test
2. mvn -pl backend/auth-service -am package -DskipTests
3. curl http://127.0.0.1:18102/health
4. curl http://127.0.0.1:18102/v3/api-docs

交接：当前任务建议保持 REVIEW，不标 DONE。

## AUTH-SERVICE-ENTERPRISE

时间：2026-05-28 11:15:00 +08:00
Agent ID：codex-auth-20260528-002
Session ID：SESSION-20260528-AUTH-SERVICE-ENTERPRISE-CODEX
分支：feature/AUTH-SERVICE-ENTERPRISE-auth-governance
状态：IN_PROGRESS

记录：
1. 已读取 AGENT.md、docs/COMMON_CONTRACTS.md、docs/agent/tasks/AUTH-SERVICE-INIT.md 和 auth-service 当前实现。
2. 用户已确认任务边界：允许修改 backend/auth-service/**，允许新增 auth-service 内文件，允许修改 auth-service 接口和 application.yml，不允许修改数据库、common、gateway、workflow、task、ai、docker、公共 DTO、MQ、Gateway。
3. 当前分支从 feature/AUTH-SERVICE-INIT-auth-service-basic 的 b671465 切出，用于基于已完成 auth 基础功能继续增强。
4. git fetch origin 失败：github.com HTTPS 连接被重置；已在任务文档记录基线风险。
5. 本机默认 java 为 JDK 21，JDK 17 路径使用 C:\Users\25611\.codex\jdks\microsoft-jdk-17.0.19\jdk-17.0.19+10。
6. 当前先进行 docs-only claim，claim push 成功后再修改 auth-service 业务代码。

文件锁申请：
1. backend/auth-service/**
2. docs/agent/tasks/AUTH-SERVICE-ENTERPRISE.md
3. docs/agent/logs/2026-05-28.md

验证计划：
1. mvn -pl backend/auth-service -am test
2. mvn -pl backend/auth-service -am package -DskipTests
3. git diff --name-only main...HEAD

阻塞：
1. docs-only claim 已本地提交：77aecc7 docs(agent): claim AUTH-SERVICE-ENTERPRISE。
2. git push -u origin feature/AUTH-SERVICE-ENTERPRISE-auth-governance 失败，无法连接 github.com:443。
3. Test-NetConnection github.com -Port 443 返回 TcpTestSucceeded=False；ssh 到 github.com:22 可连通但没有可用公钥，返回 Permission denied (publickey)。
4. 已将任务标记为 BLOCKED；claim push 成功前不修改 backend/auth-service 业务代码。

恢复：
1. 用户完成 GitHub SSH 登录后，已将 origin 切换为 git@github.com:chyinan/AetherFlow.git。
2. git push -u origin feature/AUTH-SERVICE-ENTERPRISE-auth-governance 已成功。
3. claim-first 前置条件已满足，任务恢复为 IN_PROGRESS。

## AUTH-SERVICE-ENTERPRISE 交接记录

时间：2026-05-28 15:16:00 +08:00
状态：REVIEW

完成内容：
1. 实现 Access Token + Refresh Token，Refresh Token 存 Redis，Refresh 时轮换 token pair 并自动续期。
2. 实现 Redis Session 管理、Token 黑名单、登录失败限制、密码错误次数限制、Redis 登录限流。
3. 实现登录审计日志与 traceId/userId/requestId MDC 日志规范。
4. 实现 auth-service 本地全局异常处理，覆盖 BusinessException、UnauthorizedException、ValidationException 与参数校验异常。
5. 新增 /auth/refresh、/auth/logout、/auth/status、/auth/metrics，增强 /auth/register 与 /auth/login 返回 token pair。
6. 完善 Swagger summary、description、example 和 application.yml 治理配置。

修改文件：
1. backend/auth-service/**
2. docs/agent/tasks/AUTH-SERVICE-ENTERPRISE.md
3. docs/agent/logs/2026-05-28.md

验证：
1. mvn -pl backend/auth-service -am test：通过，auth-service 29 tests，common 8 tests。
2. mvn -pl backend/auth-service -am package -DskipTests：通过。
3. git diff --check：通过，仅有 LF/CRLF warning。

提交：
1. 2b84bfa feat(auth): add token session governance foundation
2. e377194 feat(auth): implement enterprise token governance
3. PR：https://github.com/chyinan/AetherFlow/pull/1

遗留风险：
1. Gateway 当前黑名单前缀仍为 aetherflow:gateway:token:blacklist:，本任务按要求使用 auth:blacklist:{token}；若要 Gateway 拦截 auth-service 写入的黑名单，需要后续网关任务或配置对齐。
2. 统一运行环境 192.168.101.68 尚未联调 Redis/MySQL/Nacos 与真实接口链路。

交接：当前任务保持 REVIEW，不标 DONE。
## AUTH-SERVICE-INIT 合入检查

时间：2026-05-28 11:28:16 +08:00
状态：REVIEW

记录：
1. 已将 origin/main 合入 feature/AUTH-SERVICE-INIT-auth-service-basic，冲突仅出现在 docs/agent/logs/2026-05-28.md。
2. 已保留 AUTH 与 FRONTEND 两侧日志内容，删除冲突标记。
3. 已修正 AUTH 任务文档中的 Maven 验证命令，改为可复现的 reactor 命令。
4. 已复跑 mvn -pl backend/auth-service -am test 与 mvn -pl backend/auth-service -am package -DskipTests，均通过。
5. git diff --check 与 git diff --check --cached 均通过。
6. 统一运行环境 192.168.101.68 仍待补测 Nacos/MySQL/Redis 联调。

## AUTH-SERVICE-INIT 主线合入

时间：2026-05-28 11:32:15 +08:00
状态：REVIEW

记录：
1. feature/AUTH-SERVICE-INIT-auth-service-basic 已合入 main 并推送 origin/main。
2. 主线合入提交：5107c1c merge: feature/AUTH-SERVICE-INIT-auth-service-basic。
3. feature 合入前同步提交：cf2a3f9 merge: sync AUTH-SERVICE-INIT with main。
4. 代码验证仍以 mvn -pl backend/auth-service -am test 与 mvn -pl backend/auth-service -am package -DskipTests 为准，均已通过。
5. 统一运行电脑验证仍未执行，后续需在 192.168.101.68 补测 Nacos/MySQL/Redis 和 auth-service 接口。

## FRONTEND-INIT-20260528

时间：2026-05-28 01:48:01 +08:00
Agent ID：chyinan
Session ID：SESSION-20260528-0148-codex-frontend-init
分支：feature/FRONTEND-INIT-20260528-frontend-init
状态：REVIEW

记录：
1. 已调用 frontend-design skill 工作流，并结合 Aether Calm Graph Console + AI Copilot 方向约束前端 UI。
2. 已读取 AGENT.md、FRONTEND DESIGN.md、docs/COMMON_CONTRACTS.md、frontend/README.md。
3. 已确认任务边界完整：业务代码只允许修改 frontend/**；不允许修改后端、python-ai-service、docker、根 pom.xml、docker-compose.yml、AGENT.md、FRONTEND DESIGN.md、docs/COMMON_CONTRACTS.md。
4. 已完成环境检测：git 2.53.0.windows.3，Java 11.0.31，Maven 3.9.9，Node v24.15.0，npm 11.12.1，Windows 11 amd64。
5. 初始分支 feature/FRONTEND-DESIGN-20260528-baseline 工作区干净。
6. FRONTEND DESIGN.md 当前只存在于 feature/FRONTEND-DESIGN-20260528-baseline@226f3f8，不在 local main/origin/main；本任务已读取设计基线，不修改该文件，不把设计基线提交混入本任务。
7. git fetch origin --prune 失败，原因：GitHub 连接被重置。
8. 已切换 local main；git pull origin main 失败，原因：GitHub 连接被重置。
9. 已从 local main 创建任务分支 feature/FRONTEND-INIT-20260528-frontend-init。
10. AI协同项目进度.md 在 local main 不存在，且不在本任务允许新增位置内；本次 claim 不创建该文件，进度记录写入任务文档和本日志。

文件锁申请：
1. frontend/**
2. docs/agent/tasks/FRONTEND-INIT-20260528.md
3. docs/agent/logs/2026-05-28.md

验证计划：
1. node -v
2. npm -v
3. npm install
4. npm run build

阻塞预案：
1. 如果 docs-only claim push 失败，任务按 AGENT.md 停止业务编码，并在任务文档和日志中记录 BLOCKED。
2. 如果发现文件锁冲突、Git 冲突或任务边界不完整，立即停止并报告。

实施记录：
1. docs-only claim commit 已完成：10eb590。
2. git push -u origin feature/FRONTEND-INIT-20260528-frontend-init 成功，claim push 完成。
3. 已在 claim push 成功后开始修改 frontend/**。
4. 已新增 Vue3 + Vite + TypeScript 工程配置、Tailwind token、Pinia、Router、Vue Flow、Lucide icons、Axios service 层和 mock/realtime 分层。
5. 已实现 Login、AppShell、Workflow、Runs、Files、Settings 基础页面。
6. 已实现 Node Palette、WorkflowCanvas、WorkflowNode、NodeInspector、RunConsole、AICopilotPanel、RunTimeline、LogStream、FileUploader、FileAssetList。
7. 已完成业务提交并推送：b672b1d feat(frontend): initialize workflow console scaffold。

验证结果：
1. node -v：通过，v24.15.0。
2. npm -v：通过，11.12.1。
3. npm install：通过，added 219 packages。
4. npm run build：首次失败，原因是 Vue Flow Node.data 可选类型与业务节点必填数据冲突，以及 vue-tsc 对 node tsconfig 产生配置构建副产物。
5. 已在 frontend/** 内修复：新增本项目 WorkflowGraphNode/WorkflowGraphEdge 类型，tsconfig.node.json 设置 noEmit，并删除生成的 vite/tailwind js/d.ts 副产物。
6. npm run build：通过，vue-tsc -b && vite build 成功。
7. git diff --check：通过，仅有 CRLF 提示。
8. axios 检查：通过，页面和组件未直接调用 axios。
9. Element Plus 检查：通过，未引入 Element Plus。

交接：
任务ID：FRONTEND-INIT-20260528
完成内容：AetherFlow 前端基础工程已初始化，具备 mock 登录、主应用 Shell、Vue Flow workflow 工作台、AI Copilot、Runs、Files、Pinia stores、API mock 和 realtime mock。
修改文件：frontend/**、docs/agent/tasks/FRONTEND-INIT-20260528.md、docs/agent/logs/2026-05-28.md
测试结果：node -v、npm -v、npm install、npm run build 均已执行；最终 npm run build 通过。
PR/提交/分支：feature/FRONTEND-INIT-20260528-frontend-init，10eb590、b672b1d。
合入 main：未合入
统一运行电脑验证：未运行
遗留问题：FRONTEND DESIGN.md 当前未在 main；真实后端对接时只替换 services/api 和 services/realtime 内部实现。
下一步：负责人 Review 后创建 PR 或合入；后端基础功能完成后在统一运行电脑联调。
文件锁：RELEASED

Review 调整：
1. 用户澄清 AI协同项目进度.md 是旧文件名，实际对应 AGENT.md；无需新增旧名文件。
2. 已按反馈新增 /projects 登录后首页，作为项目管理和多个 workflow 的入口。
3. 已新增 frontend/src/pages/projects/ProjectsPage.vue、projectApi、projectMock、projectStore、Project 类型。
4. 已调整 Login、Router、Sidebar，登录和根路径默认进入 /projects。
5. 已扩展 workflow mock 为多个 workflow，并让 WorkflowPage 根据路由 id 加载对应 mock workflow。
6. 已增强 Workflow 画布网格，使用更明确的 24px 小网格和 120px 主网格。
7. npm run build：通过，vue-tsc -b && vite build 成功；产物 JS 372.66 kB，CSS 26.12 kB。
8. Review 调整提交已本地完成：2e00329、e57370b。
9. git push 连续失败：两次 Recv failure: Connection was reset；一次 Failed to connect to github.com port 443 after 21047 ms。当前本地分支领先远端 2 个提交，需要网络恢复后补推。

截图问题修复：
1. 用户截图指出空白主区域、右侧边界溢出、Sidebar 两个按钮同时高亮。
2. 诊断确认 AppShell 网格自动排布是主区域错位和溢出的根因；Models 与 Settings 共用 /settings 是双高亮根因。
3. 已修复 AppShell 显式 grid 位置。
4. 已新增 ModelsPage 和 /models 路由，将 Sidebar Models 从 /settings 改为 /models。
5. npm run build：通过，vue-tsc -b && vite build 成功。
6. Invoke-WebRequest http://127.0.0.1:5173/settings：200；http://127.0.0.1:5173/models：200。

Runs 页面布局修复：
1. 用户截图指出 Runs 页面位置分配有问题，节点执行区被挤压，日志区占位过大。
2. 诊断确认三列 grid 直接放列表、节点详情、日志时，日志内容最小宽度会挤压中间列。
3. 已将 RunsPage 改为两层布局：左侧 Run 列表，右侧详情工作区；详情工作区内部再分节点执行和日志。
4. 已为 RunTimeline、LogStream 补齐 min-w-0 与 overflow 约束；日志行改为固定 time/level/nodeId 列和可换行 message 列。
5. npm run build：通过，vue-tsc -b && vite build 成功。
6. Invoke-WebRequest http://127.0.0.1:5173/runs：200。
7. Runs 布局修复提交已本地完成：87ce740、d0a07ba。
8. git push 连续失败：Recv failure: Connection was reset；HTTP/1.1 重试失败，无法连接 github.com:443。当前本地分支领先远端，需要网络恢复后补推。

Workflow 工作台拥挤问题修复：
1. 用户截图反馈 AI Copilot 常驻右栏、Node Inspector、底部日志同时出现导致工作区拥挤。
2. 已取消 AppShell 全局 AI Copilot 第三列，释放主工作区宽度。
3. 已将 AI Copilot 入口移动到 Node Inspector，点击后在 Workflow 工作区右侧以抽屉弹出。
4. 已将 Run Console 从底部常驻区改为 Node Inspector 入口触发的右侧浮层。
5. 已清理 uiStore 中不再使用的 copilotCollapsed/toggleCopilot。
6. npm run build：通过，vue-tsc -b && vite build 成功。
7. Invoke-WebRequest http://127.0.0.1:5173/workflows/wf-media-digest：200。

Node Inspector 重复按钮修复：
1. 用户截图指出 Node Inspector 中内容区 Copilot/Logs 两个大按钮与标题栏图标入口重复。
2. 已移除内容区两个大按钮，保留标题栏图标入口。
3. npm run build：通过，vue-tsc -b && vite build 成功。

Models / Settings mock 细化：
1. 用户要求细化 Models 和 Settings 页面，先全部 mock。
2. 已新增 Models mock 分层：modelApi、modelMock、modelStore、Model 类型定义。
3. 已新增 Settings mock 分层：settingsApi、settingsMock、settingsStore、Settings 类型定义。
4. Models 页面已从占位页扩展为模型运行管理控制台，包含 provider 汇总、runtime pool、routing policy、model catalog、runtime logs 和 provider snapshot。
5. Settings 页面已从占位页扩展为工作区设置控制台，包含 General、Members、Environment、Integrations、Audit 五个 mock 分区。
6. 页面仍通过 services/api 和 Pinia 获取数据，没有直接调用 axios，也没有把 mock 数据写散在页面组件中。
7. Invoke-WebRequest http://127.0.0.1:5173/models：200；http://127.0.0.1:5173/settings：200。
8. node -v：v24.15.0；npm -v：11.12.1；npm install：up to date。
9. npm run build：通过，vue-tsc -b && vite build 成功。
10. Models / Settings mock 细化提交已推送：c64c7f9 feat(frontend): expand models and settings mocks。

## FRONTEND-I18N-ZH-CN-20260528

时间：2026-05-28 09:53:10 +08:00
Agent ID：chyinan
Session ID：SESSION-20260528-0953-codex-i18n-zh-cn
分支：feature/FRONTEND-I18N-ZH-CN-20260528
状态：IN_PROGRESS

记录：
1. 用户确认任务边界：为 frontend 加入多语言支持，先加入简体中文。
2. 当前工作区干净，feature/FRONTEND-INIT-20260528-frontend-init 已与远端同步。
3. 因 frontend 初始化成果尚未合入 main，本任务从 feature/FRONTEND-INIT-20260528-frontend-init 切出 feature/FRONTEND-I18N-ZH-CN-20260528。
4. 已完成环境检测：git 2.53.0.windows.3，Java 11.0.31，Maven 3.9.9，Node v24.15.0，npm 11.12.1，Windows 11 amd64。
5. 已确认任务边界完整：允许修改 frontend/**、docs/agent/tasks/FRONTEND-I18N-ZH-CN-20260528.md、docs/agent/logs/2026-05-28.md；不允许修改后端、python-ai-service、docker、根 pom.xml、docker-compose.yml、AGENT.md、FRONTEND DESIGN.md、docs/COMMON_CONTRACTS.md。

文件锁申请：
1. frontend/**
2. docs/agent/tasks/FRONTEND-I18N-ZH-CN-20260528.md
3. docs/agent/logs/2026-05-28.md

验证计划：
1. node -v
2. npm -v
3. npm install
4. npm run build

阻塞预案：
1. 如果 docs-only claim push 失败，任务按 AGENT.md 停止业务编码。
2. 如果发现文件锁冲突、Git 冲突或任务边界不完整，立即停止并报告。

实施记录：
1. docs-only claim commit 已完成并推送：33d2877 docs(agent): claim FRONTEND-I18N-ZH-CN-20260528。
2. claim push 成功后开始修改 frontend/**。
3. 已安装 vue-i18n，新增 i18n 初始化、locale 持久化、zh-CN/en-US 文案资源。
4. 已将 locale 状态接入 uiStore，并新增 LocaleSwitcher，用于登录页和顶部状态栏。
5. 已在 router 中使用 titleKey 和 i18n 设置本地化 document.title。
6. 已将导航、顶部状态栏、登录页、Projects、Workflow、Runs、Files、Models、Settings、Copilot、工作流组件、文件组件和状态组件的主要静态文案接入 i18n。
7. mock 数据中的业务名称、文件名、日志内容、模型名和节点名暂保留在 mock/service 层，不在页面组件里硬翻译。
8. 业务代码提交已完成：46ea47a feat(frontend): add zh-CN i18n support。
9. handoff 文档提交已完成：600e48e docs(agent): handoff FRONTEND-I18N-ZH-CN-20260528。
10. git push origin feature/FRONTEND-I18N-ZH-CN-20260528 首次失败：GitHub 连接被重置。
11. git push origin feature/FRONTEND-I18N-ZH-CN-20260528 第二次失败：无法连接 github.com:443。当前远端仍停留在 claim commit，需网络恢复后补推本分支所有本地提交。

验证结果：
1. node -v：v24.15.0。
2. npm -v：11.12.1。
3. npm install：通过；安装 vue-i18n 及依赖后复跑结果为 up to date。
4. npm run build：通过，vue-tsc -b && vite build 成功。
5. 构建产物：dist/index.html 0.44 kB，CSS 28.93 kB，JS 519.14 kB。
6. Vite 输出 chunk size warning：单个 JS chunk 超过 500 kB；当前为非阻塞构建警告，后续可单独做路由动态导入和 chunk 拆分。
7. git diff --check：通过，仅有 Windows CRLF 提示，无 whitespace error。

交接：
任务ID：FRONTEND-I18N-ZH-CN-20260528
完成内容：前端已接入 vue-i18n，多语言资源集中在 frontend/src/i18n，默认 zh-CN；主页面、导航、状态栏和基础组件已使用 i18n key。
修改文件：frontend/**、docs/agent/tasks/FRONTEND-I18N-ZH-CN-20260528.md、docs/agent/logs/2026-05-28.md
测试结果：node -v、npm -v、npm install、npm run build 均已执行；最终 build 通过，存在非阻塞 chunk warning。
PR/提交/分支：feature/FRONTEND-I18N-ZH-CN-20260528，代码提交 46ea47a；远端补推待网络恢复。
合入 main：未合入
统一运行电脑验证：未运行
遗留问题：mock 数据完全本地化和 chunk 拆分可后续单独处理。
下一步：网络恢复后补推 feature/FRONTEND-I18N-ZH-CN-20260528；负责人 Review，按依赖先合入 frontend-init 后再合入本 i18n 分支。
文件锁：RELEASED

## AUTH-SERVICE-ENTERPRISE Review 修复记录

时间：2026-05-28 16:08:30 +08:00
状态：REVIEW

记录：
1. 已修复黑名单契约：auth-service 同时写入 `auth:blacklist:{token}` 与 Gateway 可识别的 `aetherflow:gateway:token:blacklist:{sha256(cleanToken)}`，避免已撤销 token 继续通过网关。
2. 已将 `origin/main` 合入当前分支，并手动解决 `docs/agent/logs/2026-05-28.md` 的合并冲突，PR 分支已对齐最新 main。
3. 已重新运行 `mvn -pl backend/auth-service -am test` 与 `mvn -pl backend/auth-service -am package -DskipTests`，均通过。
4. 当前 PR 仍保持 REVIEW，等待负责人重新审阅。

交接：
1. 当前分支可继续提交到同一 PR。
2. 统一运行环境 192.168.101.68 仍建议补测真实 Redis/MySQL/Nacos 链路和 auth 接口。

## AUTH-SERVICE-ENTERPRISE Logout Review 修复

时间：2026-05-28 16:19:40 +08:00
状态：REVIEW

记录：
1. 已修复 logout 撤销语义：先读取 `auth:token:{userId}` 的当前会话 Access Token，再结合请求体 accessToken 进行黑名单写入；请求体传错时仍会撤销真正生效的 token。
2. 已补充回归测试 `logoutBlacklistsStoredAccessTokenWhenRequestAccessTokenDiffers`，锁定“请求体 token 不等于 Redis 会话 token 时，Redis 当前 token 也必须被 blacklist”的行为。
3. `mvn -pl backend/auth-service -am test` 已重新执行并通过，当前 auth-service 测试数为 30。
4. 修复提交：d48723e fix(auth): blacklist stored token on logout。

交接：
1. 当前 PR 仍需负责人重新审阅。
2. 后续如继续打回，优先看 logout 撤销语义和 Redis 会话 token 对齐。

## WORKFLOW-RUNTIME-CORE-20260528 Claim

时间：2026-05-28 17:05:15 +08:00
状态：IN_PROGRESS

任务目标：
1. 新增独立 workflow-runtime-api 协议模块。
2. 在 workflow-service 内实现 Workflow Runtime Core、DAG 调度、状态机、Retry、RuntimeEvent、Metrics 与 Observability。
3. 保证 Runtime 与业务节点彻底解耦，不实现 Whisper、Summary、Export、Notify 业务逻辑。

任务边界：
1. 允许修改 backend/workflow-runtime-api/**、backend/workflow-service/**、根 pom.xml、docs/superpowers/specs/**、docs/superpowers/plans/**、docs/agent/tasks/WORKFLOW-RUNTIME-CORE-20260528.md、docs/agent/logs/2026-05-28.md、AGENT.md。
2. 禁止修改 ai-service、gateway-service、auth-service、file-service、task-service、common、docker、frontend、python-ai-service、performance-test。
3. 禁止修改公共 DTO、既有 MQ 契约和数据库表结构。

环境检测：
1. git：git version 2.53.0.windows.3。
2. java：openjdk version "17.0.19" 2026-04-21 LTS。
3. maven：Apache Maven 3.9.9，Java version 17.0.19。
4. node：v24.15.0。
5. npm：11.12.1。
6. 检测时间：2026-05-28 17:05:15 +08:00。

同步记录：
1. GitHub HTTPS 443 不可用，已按用户授权使用 SSH remote。
2. 已通过 SSH 执行 git pull origin main，main 快进到 1af89ce。
3. 已创建分支 feature/WORKFLOW-RUNTIME-CORE-20260528-runtime-core。
4. 当前只进行 docs-only claim，claim push 成功前不修改业务代码。

验证计划：
1. git diff --name-only main...HEAD。
2. git diff --check。
3. JAVA_HOME=C:\Program Files\Microsoft\jdk-17.0.19.10-hotspot; mvn -pl backend/workflow-runtime-api,backend/workflow-service -am test。
4. 统一运行电脑补测 workflow-service 启动与 Runtime 观测接口。

## WORKFLOW-RUNTIME-CORE-20260528 Handoff

时间：2026-05-28 17:46:11 +08:00
状态：REVIEW

完成内容：
1. 新增 workflow-runtime-api 协议模块，提供 NodeExecutor、WorkflowContext、RuntimeEvent、NodeType、NodeResult、RuntimeState、RetryPolicy、NodeRegistry。
2. workflow-service 新增 Runtime Core：DefaultWorkflowContext、RuntimeStateMachine、WorkflowDag、WorkflowRuntimeEngine。
3. Runtime Engine 支持 DAG 顺序兜底、显式 next/nextNodes、branches/defaultNext、RetryPolicy、RuntimeEvent、Metrics、Observability。
4. 新增 /workflow/runtime/metrics、/workflow/runtime/observability/{workflowId}、/workflow/runtime/events/{workflowId}。
5. RuntimeEvent 支持 metrics/observability 和可选 RabbitMQ 发布，RabbitMQ 默认关闭，不修改既有 MQ 契约。
6. WorkflowServiceImpl.startInstance 已接入 Runtime Engine 生命周期。
7. Runtime 日志 pattern 和 RuntimeLogContext 已支持 traceId、workflowId、nodeId、taskId。

验证结果：
1. git diff --name-only main...HEAD：通过，修改范围符合文件锁。
2. git diff --check：通过，无 whitespace error。
3. JAVA_HOME=C:\Program Files\Microsoft\jdk-17.0.19.10-hotspot; mvn -pl backend/workflow-runtime-api,backend/workflow-service -am test：通过，common 8 tests；workflow-runtime-api 10 tests；workflow-service 21 tests；BUILD SUCCESS。

交接：
任务ID：WORKFLOW-RUNTIME-CORE-20260528
修改文件：backend/workflow-runtime-api/**；backend/workflow-service/**；pom.xml；docs/superpowers/**；docs/agent/tasks/WORKFLOW-RUNTIME-CORE-20260528.md；docs/agent/logs/2026-05-28.md；AGENT.md
PR/提交/分支：feature/WORKFLOW-RUNTIME-CORE-20260528-runtime-core；claim fa92b7c；当前业务最新 1e339fd
合入 main：未合入
统一运行电脑验证：未运行
遗留问题：真实业务节点需后续按 workflow-runtime-api 实现并注册 NodeExecutor；统一运行电脑需补测 workflow-service 启动与 Runtime REST API。
文件锁：RELEASED

## WORKFLOW-RUNTIME-CORE-20260528 Review Fix Reopen

时间：2026-05-28 17:48:00 +08:00
状态：IN_PROGRESS

记录：
1. 本地自审发现 NodeExecutor 默认 nodeType 抛错不符合协议刚性、WorkflowCompleted progress 应为 100%、Rabbit disabled 测试断言不够严、retry error message 需要兼容 null。
2. 已恢复 WORKFLOW-RUNTIME-CORE-20260528 文件锁为 ACTIVE，修复后重新验证并释放。

## WORKFLOW-RUNTIME-CORE-20260528 Review Fix Handoff

时间：2026-05-28 17:51:19 +08:00
状态：REVIEW

修复内容：
1. NodeExecutor.nodeType() 改为强制实现，避免节点开发者遗漏 node type。
2. WorkflowCompleted 后 observability progress 返回 1.0。
3. RabbitMQ disabled 测试改为 verifyNoInteractions。
4. Runtime retry event error message 兼容 null。

验证：
1. git diff --check：通过，无 whitespace error。
2. JAVA_HOME=C:\Program Files\Microsoft\jdk-17.0.19.10-hotspot; mvn -pl backend/workflow-runtime-api,backend/workflow-service -am test：通过，common 8 tests；workflow-runtime-api 10 tests；workflow-service 21 tests；BUILD SUCCESS。

交接：
1. 最新业务修复提交：2e1ff37 fix(workflow): tighten runtime protocol review findings。
2. 文件锁：RELEASED。

## WORKFLOW-RUNTIME-CORE-20260528 Main Merge

时间：2026-05-28 18:20:00 +08:00
状态：DONE

记录：
1. 已按负责人指令将 feature/WORKFLOW-RUNTIME-CORE-20260528-runtime-core 合入 main。
2. main 合入前已执行 git fetch origin、git switch main、git pull --ff-only origin main。
3. 合并命令：git merge --no-ff feature/WORKFLOW-RUNTIME-CORE-20260528-runtime-core -m "merge: workflow runtime core"。
4. 合并结果：无冲突，主线合并提交 df6893c merge: workflow runtime core。
5. git diff --check HEAD^..HEAD：通过，无 whitespace error。
6. JAVA_HOME=C:\Program Files\Microsoft\jdk-17.0.19.10-hotspot; mvn -pl backend/workflow-runtime-api,backend/workflow-service -am test：通过，common 8 tests；workflow-runtime-api 10 tests；workflow-service 21 tests；BUILD SUCCESS。

交接：
任务ID：WORKFLOW-RUNTIME-CORE-20260528
完成内容：Workflow Runtime Platform Core 已合入 main，包含 workflow-runtime-api 协议模块、workflow-service Runtime Core、DAG、Retry、Event、Metrics、Observability 和 Runtime 生命周期接入。
修改文件：backend/workflow-runtime-api/**；backend/workflow-service/**；pom.xml；docs/superpowers/**；docs/agent/tasks/WORKFLOW-RUNTIME-CORE-20260528.md；docs/agent/logs/2026-05-28.md；AGENT.md
测试结果：main 上 mvn -pl backend/workflow-runtime-api,backend/workflow-service -am test 通过；git diff --check HEAD^..HEAD 通过。
PR/提交/分支：feature/WORKFLOW-RUNTIME-CORE-20260528-runtime-core；2bdf8b2 handoff；df6893c main merge。
合入 main：已合入
统一运行电脑验证：未运行，需在 192.168.101.68 补测 workflow-service 启动、Runtime REST API、真实节点注册链路。
遗留问题：真实业务节点需后续按 workflow-runtime-api 实现并注册 NodeExecutor。
下一步：统一运行电脑 pull main 后补测 workflow-service 与 Runtime 观测接口。
文件锁：RELEASED。

## WORKFLOW-RUNTIME-RELIABILITY-20260528 Claim

时间：2026-05-28 18:36:13 +08:00
状态：IN_PROGRESS

任务目标：
1. 在已合入 main 的 Workflow Runtime Core 基础上补齐企业级 Runtime Reliability。
2. 按四阶段执行：并行 DAG + join、Runtime 持久化与恢复、Event Stream、分布式锁。
3. 第一阶段先实现真实 DAG 并行分支调度和 fan-in join，保持 Runtime 与 Node 彻底解耦。

任务边界：
1. 允许修改 backend/workflow-runtime-api/**、backend/workflow-service/**、pom.xml 如确实需要、docs/superpowers/**、docs/agent/tasks/WORKFLOW-RUNTIME-RELIABILITY-20260528.md、docs/agent/logs/2026-05-28.md、AGENT.md。
2. 禁止修改 ai-service、gateway-service、auth-service、file-service、task-service、docker、frontend、backend/common、公共 DTO、既有 MQ 契约、Whisper/Summary/Export/Notify 业务逻辑。
3. Runtime 可靠性新增表、Redis Key 和 Runtime API/协议类型已在 AGENT.md 契约表登记为 APPROVED；实现前仍会在设计文档说明表设计和原因。

环境检测：
1. git：git version 2.53.0.windows.3。
2. java：openjdk version "17.0.19" 2026-04-21 LTS。
3. maven：Apache Maven 3.9.9，Java version 17.0.19。
4. node：v24.15.0。
5. npm：11.12.1。
6. 检测时间：2026-05-28 18:36:13 +08:00。

同步记录：
1. 已读取 AGENT.md、docs/COMMON_CONTRACTS.md 和 main 上的 workflow-runtime-api / workflow-service Runtime Core。
2. 当前 main 已通过 git pull --ff-only origin main 确认为最新。
3. 已创建分支 feature/WORKFLOW-RUNTIME-RELIABILITY-20260528-runtime-reliability。
4. 当前只进行 docs-only claim，claim push 成功前不修改业务代码。

文件锁申请：
1. backend/workflow-runtime-api/**
2. backend/workflow-service/**
3. pom.xml
4. docs/superpowers/**
5. docs/agent/tasks/WORKFLOW-RUNTIME-RELIABILITY-20260528.md
6. docs/agent/logs/2026-05-28.md
7. AGENT.md

验证计划：
1. git diff --check。
2. JAVA_HOME=C:\Program Files\Microsoft\jdk-17.0.19.10-hotspot; mvn -pl backend/workflow-runtime-api,backend/workflow-service -am test。

## WORKFLOW-RUNTIME-RELIABILITY-20260528 Phase 1

时间：2026-05-28 19:55:32 +08:00
状态：IN_PROGRESS

完成内容：
1. 已完成 docs-only claim 并推送：f95a25f docs(agent): claim WORKFLOW-RUNTIME-RELIABILITY-20260528。
2. 已写入并推送 Runtime Reliability 设计文档和实施计划：03ed440 docs(workflow): plan runtime reliability。
3. WorkflowDag 已扩展为 DAG 图模型，支持 startNodeIds、nodeIds、predecessorNodeIds、requiredPredecessorCount。
4. WorkflowRuntimeEngine 已从单线程 readyQueue 改为 CompletionService 并行调度，支持 fan-out 并发执行。
5. fan-in join 通过 predecessor counter 等待所有前置节点完成后再调度。
6. 并行分支失败继续由 Runtime 统一使用 RetryPolicy；retry 耗尽后 Runtime 失败并阻止 join 调度。
7. NodeResult.nextNodeId 现在必须命中 DAG 已声明边，业务节点不能任意跳转。
8. 第一阶段代码已提交并推送：dd85e64 feat(workflow): add parallel dag join scheduling。

TDD 记录：
1. WorkflowDagTest 先失败于 DAG 图查询方法缺失，补齐图索引后通过。
2. executesFanOutBranchesConcurrently 先失败于 LEFT/RIGHT 串行执行，补齐并行调度后通过。
3. fanInJoinWaitsForEveryPredecessor、retriesFailedParallelBranchAndThenRunsJoin、failsWorkflowWhenParallelBranchExhaustsRetryAndDoesNotRunJoin 已补齐并通过。

验证：
1. git diff --check：通过，无 whitespace error。
2. JAVA_HOME=C:\Program Files\Microsoft\jdk-17.0.19.10-hotspot; mvn -pl backend/workflow-runtime-api,backend/workflow-service -am test：通过，common 8 tests；workflow-runtime-api 10 tests；workflow-service 28 tests；BUILD SUCCESS。

剩余阶段：
1. Runtime 持久化与恢复未开始。
2. Event Stream 持久化未开始。
3. 分布式锁未开始。
4. 任务整体保持 IN_PROGRESS，不标记 DONE。
