# 2026-05-29 Agent Log

## FINAL-INTEGRATION-STABILIZATION-20260529-P1-AI-FAILOVER-UI Claim

时间：2026-05-29 22:36:00 +08:00
Agent ID：chyinan
Session ID：SESSION-20260529-FINAL-INTEGRATION-P1-AI-FAILOVER
分支：feature/FINAL-INTEGRATION-STABILIZATION-20260529-p1-ai-failover-ui
状态：IN_PROGRESS

任务目标：

1. Models 页面从后端 AI Provider 契约读取真实 catalog、status、policy、metrics、logs。
2. 前端正确展示 OpenAI fail、Ollama fallback、Provider switch/recover 状态和日志。
3. 补齐 loading、error、retry、mock fallback 标识，避免演示时白屏或按钮无反应。
4. 保持 Whisper/LLM 主线真实运行，不新增主链路 mock。

文件锁：

1. frontend/src/api/modules/ai.ts
2. frontend/src/api/mappers/aiMapper.ts
3. frontend/src/services/api/modelApi.ts
4. frontend/src/stores/modelStore.ts
5. frontend/src/pages/models/ModelsPage.vue
6. frontend/src/types/model.ts
7. frontend/src/i18n/locales/zh-CN.ts
8. frontend/src/i18n/locales/en-US.ts
9. docs/agent/tasks/FINAL-INTEGRATION-STABILIZATION-20260529-P1-AI-FAILOVER-UI.md
10. docs/agent/logs/2026-05-29.md
11. AGENT.md

验证计划：

1. git diff --name-only main...HEAD
2. git diff --check
3. rg -n "^(<<<<<<<|=======|>>>>>>>)" AGENT.md docs/agent/logs/2026-05-29.md frontend/src
4. cd frontend; npm run build
5. 统一运行电脑补测真实 Gateway `/ai/provider/**`、OpenAI fail -> Ollama fallback、Provider switch/recover。

## FINAL-INTEGRATION-STABILIZATION-20260529-P1-AI-FAILOVER-UI Completion

时间：2026-05-29 22:50:00 +08:00
Agent ID：chyinan
Session ID：SESSION-20260529-FINAL-INTEGRATION-P1-AI-FAILOVER
分支：feature/FINAL-INTEGRATION-STABILIZATION-20260529-p1-ai-failover-ui
状态：REVIEW

完成内容：

1. 前端 AI Provider 模块补齐 `/ai/provider/catalog`、`/ai/provider/logs?limit=`、`requestTimeout` 与 catalog/log DTO。
2. Models 页面从真实 catalog/status/policy/metrics/logs 合并出 provider cards、model catalog、routing policy 和 runtime logs。
3. UI 增加真实/安全回退来源、loading、error、retry、empty state、设为主路由、恢复熔断、health/circuit/provider order。
4. 保留 UI mock fallback 只作为后端不可用时的安全兜底，不影响 Whisper/LLM 主线真实运行。
5. 未修改 backend、python-ai-service、Docker、Runtime Core、接口、DTO、数据库。

验证记录：

1. `cd frontend; npm run build` 通过：vue-tsc 与 Vite build 通过，仅既有 chunk size warning。
2. `git diff --name-only main...HEAD` 通过：修改范围在本任务文件锁内。
3. `git diff --check main...HEAD` 通过。
4. `rg -n "^(<<<<<<<|=======|>>>>>>>)" AGENT.md docs/agent/logs/2026-05-29.md frontend/src` 通过：无冲突标记输出。

提交：

1. 27fd196 docs(agent): claim FINAL-INTEGRATION-STABILIZATION-20260529-P1-ai-failover-ui
2. 2b60a28 fix(frontend): stabilize ai failover models ui

统一运行电脑验证：未运行。

遗留问题：

1. 需统一运行电脑补测真实 Gateway `/ai/provider/**`、OpenAI fail -> Ollama fallback、Provider switch/recover。
2. Demo 前需先跑一次 Summary/LLM 请求产生日志，否则 `/ai/provider/logs` 可能为空。

文件锁：RELEASED。

## FRONTEND-PUBLIC-HOME-LOGIN Main Merge

时间：2026-05-29 20:31:00 +08:00
Agent ID：chyinan
Session ID：SESSION-20260529-2031-FRONTEND-PUBLIC-HOME-MERGE
分支：main
状态：DONE

记录：
1. 按负责人指令合入 `feature/FRONTEND-PUBLIC-HOME-LOGIN-public-home-login` 的公开首页与登录页纯页面样式。
2. `AGENT.md` 与 `docs/agent/logs/2026-05-29.md` 发生协作文档冲突，处理方式为保留当前 main 已有后端合入记录，并追加 FRONTEND-PUBLIC-HOME-LOGIN 的看板、文件锁、验证和交接记录。
3. 保留分支的 `/` 公开首页、`/login` GitHub 风格居中模板、i18n 文案和路由变更。
4. 修正 `LoginPage.vue` 中页面层直接构造并写入 token session 的实现，第三方模板入口回到现有 `authStore.login()` / authApi mock fallback，避免页面绕过 token 生命周期。
5. 不修改后端接口、DTO、数据库、Redis、MQ、Nacos、Gateway、package 配置或登录后的业务页面。

验证结果：
1. `cd frontend && npm run build`：通过，vue-tsc 与 Vite build 通过；仅既有 chunk size warning。
2. `git diff --check`：通过，无 whitespace error。
3. `rg -n "^(<<<<<<<\|=======\|>>>>>>>)" AGENT.md docs/agent/logs/2026-05-29.md frontend`：通过，无冲突标记。

统一运行电脑验证：未运行。

遗留问题：
1. GitHub / Google 仍是前端模板入口，未接真实 OAuth。
2. 需统一运行电脑补测浏览器 `/`、`/login` 和 mock 登录跳转。

文件锁：RELEASED。

## FINAL-INTEGRATION-STABILIZATION-20260529-P1-DOCKER-DEMO-SAFE-MODE Claim

时间：2026-05-29 22:28:00 +08:00
Agent ID：chyinan
Session ID：SESSION-20260529-FINAL-INTEGRATION-P1-DOCKER
分支：feature/FINAL-INTEGRATION-STABILIZATION-20260529-p1-docker-demo-safe-mode
状态：IN_PROGRESS

任务目标：

1. Docker demo 默认保持 Whisper/LLM 主线能力真实运行，同时保留环境变量显式关闭能力。
2. `MINIO_PUBLIC_ENDPOINT` 改为可配置默认 localhost。
3. Java 服务补齐 `OLLAMA_BASE_URL`，前端 Docker build 补齐已有 Vite 环境变量。

文件锁：

1. docker-compose.yml
2. frontend/nginx/Dockerfile
3. frontend/.env.example
4. docs/agent/tasks/FINAL-INTEGRATION-STABILIZATION-20260529-P1-DOCKER-DEMO-SAFE-MODE.md
5. docs/agent/logs/2026-05-29.md
6. AGENT.md

验证计划：

1. git diff --name-only main...HEAD
2. git diff --check
3. rg -n "^(<<<<<<<|=======|>>>>>>>)" AGENT.md docs/agent/logs/2026-05-29.md docker-compose.yml frontend/nginx/Dockerfile frontend/.env.example
4. docker compose config --quiet
5. cd frontend; npm run build

统一运行电脑补测：docker compose up 与真实 Demo chain。

## FINAL-INTEGRATION-STABILIZATION-20260529-P1-DOCKER-DEMO-SAFE-MODE Completion

时间：2026-05-29 22:40:00 +08:00
Agent ID：chyinan
Session ID：SESSION-20260529-FINAL-INTEGRATION-P1-DOCKER
分支：feature/FINAL-INTEGRATION-STABILIZATION-20260529-p1-docker-demo-safe-mode
状态：REVIEW

完成内容：

1. `python-ai-service` 默认 `ENABLE_WHISPER=true`、`ENABLE_LLM=true`，保持主线 Whisper/LLM 演示真实运行；仍可通过环境变量显式关闭。
2. Java 服务公共 env 增加 `OLLAMA_BASE_URL`，默认 `http://host.docker.internal:11434`。
3. `MINIO_PUBLIC_ENDPOINT` 改为 `${MINIO_PUBLIC_ENDPOINT:-http://localhost:9000}`。
4. workflow-service 默认 `WORKFLOW_OCR_MOCK=true`，保护当前 Java 容器无 Tesseract 的 OCR 演示；真实 OCR 容器化拆后续任务。
5. 前端 Docker build args 补齐 `VITE_SSE_BASE`、`VITE_MOCK_FALLBACK`、`VITE_NOTIFY_WS_FALLBACK`、`VITE_API_TIMEOUT_MS`。
6. 未修改 backend、python-ai-service、业务 UI、接口、DTO、数据库、Gateway、Runtime Core。

验证记录：

1. `docker compose config --quiet` 通过。
2. `cd frontend; npm run build` 通过：vue-tsc 与 Vite build 通过，仅既有 chunk size warning。
3. `git diff --check` 通过：无 whitespace error，仅 Windows LF/CRLF 提示。
4. `rg -n "^(<<<<<<<|=======|>>>>>>>)" AGENT.md docs/agent/logs/2026-05-29.md docker-compose.yml frontend/nginx/Dockerfile frontend/.env.example` 通过：无冲突标记输出。
5. compose 展开检查确认 Whisper/LLM 默认 true。

提交：

1. c030102 docs(agent): claim FINAL-INTEGRATION-STABILIZATION-20260529-P1-docker-demo
2. f56615e chore(docker): add demo safe mode defaults
3. c8e2483 chore(docker): keep whisper and llm enabled by default

统一运行电脑验证：未运行。

遗留问题：

1. 需统一运行电脑补测 `docker compose up`、Whisper/LLM 真实链路、Ollama reachable、MinIO URL 可访问。
2. 真实 OCR 容器化/Tesseract 安装仍未做，当前 OCR demo 默认 mock。

文件锁：RELEASED。

## FINAL-INTEGRATION-STABILIZATION-20260529-P1-DOCKER-DEMO-SAFE-MODE Main Merge

时间：2026-05-29 22:32:00 +08:00
Agent ID：chyinan
Session ID：SESSION-20260529-FINAL-INTEGRATION-P1-DOCKER-MERGE
分支：main
状态：DONE

合入结果：

1. feature 分支已通过 `--no-ff` 合入 main。
2. main merge commit：83ca01a。
3. compose 展开确认 `ENABLE_WHISPER=true`、`ENABLE_LLM=true`，主线 Whisper/LLM 仍为真实运行。

验证记录：

1. `docker compose config --quiet` 通过。
2. `cd frontend; npm run build` 通过：vue-tsc 与 Vite build 通过，仅既有 chunk size warning。
3. `git diff --check HEAD^1..HEAD` 通过。
4. `rg -n "^(<<<<<<<|=======|>>>>>>>)" AGENT.md docs/agent/logs/2026-05-29.md docker-compose.yml frontend/nginx/Dockerfile frontend/.env.example` 通过：无冲突标记输出。
5. `docker compose config | rg -n "ENABLE_WHISPER|ENABLE_LLM|MINIO_PUBLIC_ENDPOINT|OLLAMA_BASE_URL|WORKFLOW_OCR_MOCK|VITE_NOTIFY_WS_FALLBACK|VITE_SSE_BASE"` 通过：关键 demo env 按预期展开。

统一运行电脑验证：未运行。

遗留问题：

1. 需统一运行电脑补测 `docker compose up`、Whisper/LLM 真实链路、Ollama reachable、MinIO URL 可访问。
2. 真实 OCR 容器化/Tesseract 安装仍未做，当前 OCR demo 默认 mock。

文件锁：RELEASED。

## FINAL-INTEGRATION-STABILIZATION-20260529-P1-CHUNK-UPLOAD Main Merge

时间：2026-05-29 22:24:00 +08:00
Agent ID：chyinan
Session ID：SESSION-20260529-FINAL-INTEGRATION-P1-CHUNK-MERGE
分支：main
状态：DONE

记录：

1. 按负责人指令将 `feature/FINAL-INTEGRATION-STABILIZATION-20260529-p1-chunk-upload` 合入 `main`。
2. 合入内容包括前端 `/files/uploads/**` 分片上传 API、50MB 阈值、8MB 分片、进度汇总和失败 abort。
3. 未修改 backend、docker、配置、DTO、数据库、Gateway、Runtime Core。

验证结果：

1. `cd frontend; npm run build`：通过，vue-tsc 与 Vite build 通过；仅既有 chunk size warning。
2. `git diff --check HEAD^..HEAD`：通过，无 whitespace error。
3. `rg -n "^(<<<<<<<|=======|>>>>>>>)" AGENT.md docs/agent/logs/2026-05-29.md frontend/src`：通过，无冲突标记。

统一运行电脑验证：未运行。

遗留问题：

1. 需统一运行电脑补测真实大文件分片上传、失败 abort、上传后 Workflow run fileId。
2. 下一步进入 Docker Demo Safe Mode / AI fallback 环境稳定。

文件锁：RELEASED。

## FINAL-INTEGRATION-STABILIZATION-20260529-P1-CHUNK-UPLOAD Claim

时间：2026-05-29 22:02:00 +08:00
Agent ID：chyinan
Session ID：SESSION-20260529-FINAL-INTEGRATION-P1-CHUNK
分支：feature/FINAL-INTEGRATION-STABILIZATION-20260529-p1-chunk-upload
状态：IN_PROGRESS

任务目标：

1. 前端接入后端 `/files/uploads/**` 分片上传接口。
2. 大文件自动走分片上传，小文件继续走既有 multipart upload。
3. 进度保持连续反馈，完成后保留真实 `backendFileId`。
4. 失败时 abort 分片会话；后端不可用时保留 mock fallback。

文件锁：

1. frontend/src/api/modules/file.ts
2. frontend/src/services/api/fileApi.ts
3. frontend/src/api/mappers/fileMapper.ts
4. docs/agent/tasks/FINAL-INTEGRATION-STABILIZATION-20260529-P1-CHUNK-UPLOAD.md
5. docs/agent/logs/2026-05-29.md
6. AGENT.md

验证计划：

1. git diff --name-only main...HEAD
2. git diff --check
3. rg -n "^(<<<<<<<|=======|>>>>>>>)" AGENT.md docs/agent/logs/2026-05-29.md frontend/src
4. cd frontend; npm run build

统一运行电脑补测：大文件分片上传、失败 abort、上传后 Workflow run fileId。

## FINAL-INTEGRATION-STABILIZATION-20260529-P1-CHUNK-UPLOAD Completion

时间：2026-05-29 22:15:00 +08:00
Agent ID：chyinan
Session ID：SESSION-20260529-FINAL-INTEGRATION-P1-CHUNK
分支：feature/FINAL-INTEGRATION-STABILIZATION-20260529-p1-chunk-upload
状态：REVIEW

完成内容：

1. 新增前端 chunk upload DTO 与 `initChunkUpload`、`uploadChunkPart`、`completeChunkUpload`、`abortChunkUpload` API。
2. `fileApi.uploadFile()` 对 50MB 及以上文件自动使用 8MB 分片上传，小文件保持既有 multipart。
3. 分片上传进度按已完成 part 与当前 part 上传进度汇总。
4. 分片完成后映射真实 FileAsset，保留 `backendFileId`；失败时调用 abort。
5. 未修改 backend、docker、配置、DTO、数据库、Gateway、Runtime Core。

验证记录：

1. `cd frontend; npm run build` 通过：vue-tsc 与 Vite build 通过，仅既有 chunk size warning。
2. `git diff --check` 通过：无 whitespace error，仅 Windows LF/CRLF 提示。
3. `rg -n "^(<<<<<<<|=======|>>>>>>>)" AGENT.md docs/agent/logs/2026-05-29.md frontend/src` 通过：无冲突标记输出。

提交：

1. 7f1eced docs(agent): claim FINAL-INTEGRATION-STABILIZATION-20260529-P1-chunk-upload
2. f27fef9 fix(frontend): use chunk upload for large files

统一运行电脑验证：未运行。

遗留问题：

1. 真实大文件分片上传、失败 abort、上传后 Workflow run fileId 需统一运行电脑补测。
2. Docker/nginx Demo Safe Mode 与 AI fallback 仍需后续任务处理。

文件锁：RELEASED。

## FINAL-INTEGRATION-STABILIZATION-20260529-P1-FILE-LIST-UPLOAD Main Merge

时间：2026-05-29 21:55:00 +08:00
Agent ID：chyinan
Session ID：SESSION-20260529-FINAL-INTEGRATION-P1-FILE-MERGE
分支：main
状态：DONE

记录：

1. 按负责人指令将 `feature/FINAL-INTEGRATION-STABILIZATION-20260529-p1-file-list-upload` 合入 `main`。
2. 合入内容包括前端 `GET /files` real-first、FileAssetPageResponse 映射、WorkflowPage 启动前加载文件列表。
3. 未修改 backend、docker、配置、DTO、数据库、Gateway、Runtime Core。

验证结果：

1. `cd frontend; npm run build`：通过，vue-tsc 与 Vite build 通过；仅既有 chunk size warning。
2. `git diff --check HEAD^..HEAD`：通过，无 whitespace error。
3. `rg -n "^(<<<<<<<|=======|>>>>>>>)" AGENT.md docs/agent/logs/2026-05-29.md frontend/src`：通过，无冲突标记。

统一运行电脑验证：未运行。

遗留问题：

1. 需统一运行电脑补测真实 `GET /files`、上传刷新恢复、Workflow run fileId 端到端。
2. 下一步进入大文件上传 / Demo Safe Mode 小分支。

文件锁：RELEASED。

## FINAL-INTEGRATION-STABILIZATION-20260529-P1-FILE-LIST-UPLOAD Claim

时间：2026-05-29 21:36:00 +08:00
Agent ID：chyinan
Session ID：SESSION-20260529-FINAL-INTEGRATION-P1-FILE
分支：feature/FINAL-INTEGRATION-STABILIZATION-20260529-p1-file-list-upload
状态：IN_PROGRESS

任务目标：

1. 前端 `fileApi.listFiles()` real-first 调用后端 `GET /files`。
2. 映射后端 `FileAssetPageResponse.items` 到前端 `FileAsset`。
3. WorkflowPage 启动前加载文件列表，刷新后仍可拿到真实 `backendFileId`。

文件锁：

1. frontend/src/api/modules/file.ts
2. frontend/src/api/mappers/fileMapper.ts
3. frontend/src/services/api/fileApi.ts
4. frontend/src/stores/fileStore.ts
5. frontend/src/pages/workflows/WorkflowPage.vue
6. docs/agent/tasks/FINAL-INTEGRATION-STABILIZATION-20260529-P1-FILE-LIST-UPLOAD.md
7. docs/agent/logs/2026-05-29.md
8. AGENT.md

验证计划：

1. git diff --name-only main...HEAD
2. git diff --check
3. rg -n "^(<<<<<<<|=======|>>>>>>>)" AGENT.md docs/agent/logs/2026-05-29.md frontend/src
4. cd frontend; npm run build

统一运行电脑补测：真实 `GET /files`、上传刷新恢复、Workflow run fileId 端到端。

## FINAL-INTEGRATION-STABILIZATION-20260529-P1-FILE-LIST-UPLOAD Completion

时间：2026-05-29 21:48:00 +08:00
Agent ID：chyinan
Session ID：SESSION-20260529-FINAL-INTEGRATION-P1-FILE
分支：feature/FINAL-INTEGRATION-STABILIZATION-20260529-p1-file-list-upload
状态：REVIEW

完成内容：

1. 新增前端 `GET /files` module 类型和调用。
2. 新增后端 `FileAssetPageResponse.items` 到前端 `FileAsset` 的 mapper。
3. `fileApi.listFiles()` 改为 real-first；后端不可用且 mock fallback 开启时才回退 mockFiles。
4. WorkflowPage mounted/startRun 前加载文件列表，刷新页面后可恢复真实 `backendFileId`。
5. 未修改 backend、docker、配置、DTO、数据库、Gateway、Runtime Core。

验证记录：

1. `cd frontend; npm run build` 通过：vue-tsc 与 Vite build 通过，仅既有 chunk size warning。
2. `git diff --check` 通过：无 whitespace error，仅 Windows LF/CRLF 提示。
3. `rg -n "^(<<<<<<<|=======|>>>>>>>)" AGENT.md docs/agent/logs/2026-05-29.md frontend/src` 通过：无冲突标记输出。

提交：

1. 132b422 docs(agent): claim FINAL-INTEGRATION-STABILIZATION-20260529-P1-file-list
2. 13b97b1 fix(frontend): load backend file assets for workflow runs

统一运行电脑验证：未运行。

遗留问题：

1. 真实 Gateway/file-service/MySQL/auth header 链路需统一运行电脑补测。
2. 大文件分片上传、Nginx 100MB 限制与 Docker Demo Safe Mode 拆后续任务。

文件锁：RELEASED。

## FINAL-INTEGRATION-STABILIZATION-20260529-P0-RUNTIME-FILEID-SSE-WS Main Merge

时间：2026-05-29 21:32:00 +08:00
Agent ID：chyinan
Session ID：SESSION-20260529-FINAL-INTEGRATION-P0-MERGE
分支：main
状态：DONE

记录：

1. 按负责人指令将 `feature/FINAL-INTEGRATION-STABILIZATION-20260529-p0-runtime-fileid-sse-ws` 合入 `main`。
2. 合入内容包括 Workflow run `input.fileId`、runtime id 贯通、真实 Runtime SSE、Notify WS stream token。
3. 未修改 backend、docker、配置、DTO、数据库、Gateway、Runtime Core。

验证结果：

1. `cd frontend; npm run build`：通过，vue-tsc 与 Vite build 通过；仅既有 chunk size warning。
2. `git diff --check HEAD^..HEAD`：通过，无 whitespace error。
3. `rg -n "^(<<<<<<<|=======|>>>>>>>)" AGENT.md docs/agent/logs/2026-05-29.md frontend/src`：通过，无冲突标记。

统一运行电脑验证：未运行。

遗留问题：

1. 需统一运行电脑补测真实上传后 Workflow run、Gateway Runtime SSE、Notify WS stream token。
2. 下一步进入 Upload File List / Docker Demo Safe Mode 小分支。

文件锁：RELEASED。

## FINAL-INTEGRATION-STABILIZATION-20260529-P0-RUNTIME-FILEID-SSE-WS Claim

时间：2026-05-29 21:00:00 +08:00
Agent ID：chyinan
Session ID：SESSION-20260529-FINAL-INTEGRATION-P0
分支：feature/FINAL-INTEGRATION-STABILIZATION-20260529-p0-runtime-fileid-sse-ws
状态：IN_PROGRESS

任务目标：

1. 修复 Workflow run 缺少 `StartWorkflowRequest.input.fileId`。
2. 贯通前端 run 与后端 runtime / instance id。
3. Runtime 日志订阅接入真实 `/workflow/runtime/stream/{workflowId}` SSE。
4. Notify WebSocket 改为 `POST /notify/stream-token` + `?streamToken=`。

文件锁：

1. frontend/src/api/modules/workflow.ts
2. frontend/src/api/modules/runtime.ts
3. frontend/src/api/modules/notify.ts
4. frontend/src/services/api/workflowApi.ts
5. frontend/src/services/realtime/**
6. frontend/src/stores/runStore.ts
7. frontend/src/stores/fileStore.ts
8. frontend/src/pages/workflows/WorkflowPage.vue
9. docs/agent/tasks/FINAL-INTEGRATION-STABILIZATION-20260529-P0-RUNTIME-FILEID-SSE-WS.md
10. docs/agent/logs/2026-05-29.md
11. AGENT.md

验证计划：

1. git diff --name-only main...HEAD
2. git diff --check
3. rg -n "^(<<<<<<<|=======|>>>>>>>)" AGENT.md docs/agent/logs/2026-05-29.md frontend/src
4. cd frontend; npm run build

环境检测：

1. git：2.53.0.windows.3
2. java：OpenJDK 17.0.19
3. maven：Apache Maven 3.9.9，默认 Java 11.0.31；后端验证需显式 JAVA_HOME 指向 JDK 17
4. node：v24.15.0
5. npm：11.12.1
6. 操作系统：Windows 11
7. 检测时间：2026-05-29 21:00:00 +08:00
8. 是否需要统一运行电脑补测：是，真实 Gateway/SSE/WS/Workflow run 需要统一运行环境补测。

## FINAL-INTEGRATION-STABILIZATION-20260529-P0-RUNTIME-FILEID-SSE-WS Completion

时间：2026-05-29 21:20:00 +08:00
Agent ID：chyinan
Session ID：SESSION-20260529-FINAL-INTEGRATION-P0
分支：feature/FINAL-INTEGRATION-STABILIZATION-20260529-p0-runtime-fileid-sse-ws
状态：REVIEW

完成内容：

1. WorkflowPage 从 fileStore 读取最近上传 input 文件的 `backendFileId`，并通过 workflowApi 发送到 `StartWorkflowRequest.input.fileId`。
2. 后端返回的 instance/runtime 信息直接进入 runStore，减少本地 storage 推断。
3. Runtime SSE 接入真实 `/workflow/runtime/stream/{workflowId}`，支持 heartbeat 忽略、runtime event 映射、终态关闭、`Last-Event-ID` 与显式 demo fallback。
4. Notify WebSocket 改为先取 `POST /notify/stream-token`，再使用 `?streamToken=` 建连，重连时重新取 token。
5. 未修改 backend、docker、配置、DTO、数据库、Gateway、Runtime Core。

验证记录：

1. `cd frontend; npm run build` 通过：vue-tsc 与 Vite build 通过，仅既有 chunk size warning。
2. `git diff --check` 通过：无 whitespace error，仅 Windows LF/CRLF 提示。
3. `rg -n "^(<<<<<<<|=======|>>>>>>>)" AGENT.md docs/agent/logs/2026-05-29.md frontend/src` 通过：无冲突标记输出。
4. 修改范围检查通过：修改限定在本任务文件锁范围。

提交：

1. 406faf5 docs(agent): claim FINAL-INTEGRATION-STABILIZATION-20260529-P0
2. 159b818 fix(frontend): stabilize runtime demo integrations

统一运行电脑验证：未运行。

遗留问题：

1. 真实 Gateway -> workflow-service SSE、notify-service WS stream token、Workflow run fileId 端到端需统一运行电脑补测。
2. `fileApi.listFiles()` 仍未在本任务接真实 `GET /files`，刷新页面后的历史上传文件恢复属于下一阶段 Upload Review。
3. Docker Demo Safe Mode、AI Failover UI、大文件分片上传属于后续任务。

文件锁：RELEASED。

## BE-GW-WORKFLOW-ROUTE-20260529 Claim

时间：2026-05-29 17:04:00 +08:00
Agent ID：chyinan
Session ID：SESSION-20260529-1704-BE-GW-WORKFLOW-ROUTE
分支：feature/BE-GW-WORKFLOW-ROUTE-20260529-workflow-route
状态：IN_PROGRESS

记录：
1. 已读取 AGENT.md 和 docs/COMMON_CONTRACTS.md。
2. 已读取 docs/frontend-backend-missing-apis.md、FE-API-INTEGRATION-20260529 任务文档、frontend API integration plan。
3. 已检查 workflow-service 已存在 `/workflow/runtime/**` 与 `/workflow/node/**` controller，Gateway 当前仅配置 `/workflows/**,/workflow-instances/**`，缺少 `/workflow/**`。
4. 已确认当前文件锁表中目标文件无 ACTIVE 冲突。
5. 已同步 main：`git pull origin main` 返回 Already up to date。
6. 已创建分支 `feature/BE-GW-WORKFLOW-ROUTE-20260529-workflow-route`。
7. 当前进行 docs-only claim；claim push 成功前不修改业务代码。

任务边界：
1. 允许修改 `backend/gateway-service/src/main/resources/application.yml`。
2. 允许修改 `backend/gateway-service/src/test/java/com/aetherflow/gateway/GatewayRouteConfigurationTest.java`。
3. 允许修改 `docs/agent/tasks/BE-GW-WORKFLOW-ROUTE-20260529.md`、`docs/agent/logs/2026-05-29.md`、`AGENT.md`。
4. 禁止修改 frontend、workflow-service、common、DB、Redis、MQ、Nacos 和错误码。

契约登记：
1. Gateway 路由：`/workflow/** -> workflow-service`。
2. 状态：APPROVED。
3. 只暴露 workflow-service 已实现 API，不修改 controller / DTO / service。

验证计划：
1. `git diff --check`
2. `JAVA_HOME=C:\Program Files\Microsoft\jdk-17.0.19.10-hotspot; mvn -pl backend/gateway-service -am -Dtest=GatewayRouteConfigurationTest test`
3. 如时间允许：`JAVA_HOME=C:\Program Files\Microsoft\jdk-17.0.19.10-hotspot; mvn -pl backend/gateway-service -am test`

## BE-GW-WORKFLOW-ROUTE-20260529 Implementation

时间：2026-05-29 17:11:00 +08:00
Agent ID：chyinan
Session ID：SESSION-20260529-1704-BE-GW-WORKFLOW-ROUTE
分支：feature/BE-GW-WORKFLOW-ROUTE-20260529-workflow-route
状态：REVIEW

完成内容：
1. Gateway `workflow-service` route 增加 `/workflow/**`，覆盖前端已接入的 Runtime 与 Node API。
2. Sentinel `workflow-api` pattern 增加 `/workflow`，保持 `/workflow/**` 与 `/workflows/**`、`/workflow-instances/**` 同一限流/熔断分组。
3. `GatewayRouteConfigurationTest` 新增 `/workflow/runtime/metrics`、`/workflow/runtime/observability/{id}`、`/workflow/runtime/events/{id}`、`/workflow/node/catalog`、`/workflow/node/metrics` route contract。
4. `GatewayRouteConfigurationTest` 新增 Sentinel workflow-api pattern contract。

TDD 记录：
1. RED：`workflowRuntimeAndNodePathsSelectWorkflowServiceRoute` 在未修改 Gateway route 时失败，原因是 `/workflow/runtime/metrics` 无匹配路由。
2. GREEN：增加 `/workflow/**` 后，Gateway route contract 通过。
3. RED：`workflowApiSentinelGroupIncludesSingularWorkflowPrefix` 在未修改 Sentinel pattern 时失败，原因是 workflow-api 缺 `/workflow`。
4. GREEN：增加 `/workflow` pattern 后，Gateway route contract 通过。

验证结果：
1. `git diff --check`：通过，无 whitespace error，仅 Windows LF/CRLF 提示。
2. `JAVA_HOME=C:\Program Files\Microsoft\jdk-17.0.19.10-hotspot; mvn -pl backend/gateway-service -am -Dtest=GatewayRouteConfigurationTest '-Dsurefire.failIfNoSpecifiedTests=false' test`：通过，GatewayRouteConfigurationTest 6 tests；BUILD SUCCESS。
3. `JAVA_HOME=C:\Program Files\Microsoft\jdk-17.0.19.10-hotspot; mvn -pl backend/gateway-service -am test`：通过，common 8 tests；gateway-service 18 tests；BUILD SUCCESS。

提交：
1. `e82e8b0 docs(agent): claim BE-GW-WORKFLOW-ROUTE-20260529`
2. `feat(gateway): add workflow route`

统一运行电脑验证：未运行。

遗留问题：
1. 需统一运行电脑启动 Gateway、Nacos、workflow-service 后补测真实 `/workflow/runtime/**` 与 `/workflow/node/**` 转发。
2. P0 中 Workflow definitions CRUD 和 Workflow run list/detail/log 仍待后续任务。

文件锁：RELEASED。

## FE-API-INTEGRATION-20260529 Claim

时间：2026-05-29 12:47:00 +08:00
Agent ID：chyinan
Session ID：SESSION-20260529-1247-FE-API-INTEGRATION
分支：feature/FE-API-INTEGRATION-20260529-frontend-integration
状态：IN_PROGRESS

记录：
1. 已读取 AGENT.md 和 docs/COMMON_CONTRACTS.md。
2. 已读取 frontend-design skill。
3. 已读取 Superpowers skills：using-superpowers、writing-plans、executing-plans、subagent-driven-development。
4. 已确认用户要求：先接入后端已实现 API，后端未实现但前端需要的 API 统计为后续后端任务；使用 subagents。
5. 已创建分支 `feature/FE-API-INTEGRATION-20260529-frontend-integration`。
6. 已检查当前工作区，claim 前无业务代码修改。
7. 当前进行 docs-only claim；claim push 成功前不修改业务代码。

任务边界：
1. 允许修改 frontend/src/api/**、frontend/src/services/api/**、frontend/src/services/http/**、frontend/src/services/realtime/**、frontend/src/stores/**、frontend/src/types/**、frontend/src/config/**。
2. 允许修改 frontend/package.json、frontend/package-lock.json、frontend/.env.example。
3. 允许新增 docs/frontend-backend-missing-apis.md 和 docs/superpowers/plans/2026-05-29-frontend-api-integration.md。
4. 禁止修改 backend/**、docker/**、数据库、MQ、Redis、Nacos、Gateway、错误码和后端 DTO。
5. 页面和组件默认不改；如必须改 glue code，先记录原因。

环境检测：
1. git：git version 2.53.0.windows.3。
2. java：OpenJDK 17.0.19 Microsoft。
3. maven：Apache Maven 3.9.9；默认 Java 11.0.31。
4. node：v24.15.0。
5. npm：11.12.1。
6. OS：Microsoft Windows 11 专业工作站版 10.0.26200。
7. 检测时间：2026-05-29 12:47 +08:00。
8. 不能执行的命令：无。
9. 是否需要统一运行电脑补测：是，涉及真实 Gateway 与多服务联调。

验证计划：
1. npm run build（frontend）。
2. git diff --check。
3. 如新增 SDK 生成脚本：npm run api:generate。
4. 统一运行电脑补测 Auth、Workflow、Runtime、File Upload、SSE、WebSocket、Retry、Error Handling。

## FE-API-INTEGRATION-20260529 Plan

时间：2026-05-29 12:53:00 +08:00
Agent ID：chyinan
Session ID：SESSION-20260529-1247-FE-API-INTEGRATION
分支：feature/FE-API-INTEGRATION-20260529-frontend-integration
状态：IN_PROGRESS

记录：
1. docs-only claim 已提交并推送：`3713838 docs(agent): claim FE-API-INTEGRATION-20260529`。
2. 已按 Superpowers writing-plans 写入实施计划：`docs/superpowers/plans/2026-05-29-frontend-api-integration.md`。
3. 后续执行采用 subagents，但遵守项目“一次只能单个 agent 修改代码”的要求：代码 worker 串行执行，review subagent 只审查不改代码。

## FE-API-INTEGRATION-20260529 Task 4

时间：2026-05-29 14:44:00 +08:00
Agent ID：chyinan
Session ID：SESSION-20260529-1247-FE-API-INTEGRATION
分支：feature/FE-API-INTEGRATION-20260529-frontend-integration
状态：IN_PROGRESS

完成内容：
1. 新增 `frontend/src/api/modules/file.ts`，接入 `/files/upload`、`/files/progress/{taskId}`、`/files/{id}/download`、`DELETE /files/{id}`。
2. 新增 `frontend/src/api/modules/notify.ts`，提供 Notify DTO、SSE/WS URL builder 和安全消息解析。
3. 新增 `frontend/src/api/mappers/fileMapper.ts`，把 `FileMetadataDTO` / `UploadProgressView` 映射为现有 `FileAsset`。
4. 更新 `frontend/src/services/api/fileApi.ts` 和 `frontend/src/stores/fileStore.ts`，保留 `listFiles()` mock，上传优先真实 API，网络/Gateway 不可用时按 `runtimeEnv.mockFallback` 回退 mock，业务校验/鉴权错误不回退。
5. 新增 fetch-based SSE client 和 Notify WebSocket client，支持 reconnect、heartbeat/idle recovery 和消息 parse guard。
6. 更新 `realtimeClient`，保留 `subscribeRun()` scripted run stream，新增 Notify SSE 优先、WebSocket fallback 的通知订阅。
7. 更新 `uiStore` / `runStore`，将通知 transport 状态写入既有 `realtimeState` / `notifications`，并在 run cleanup 时关闭通知订阅。

验证结果：
1. `cd frontend; npm run build`：通过。Vite 输出 chunk size warning，非 TypeScript/build failure。
2. `git diff --check`：通过，仅 Windows LF/CRLF 提示。

已知问题：
1. 后端仍缺少文件 list/query API，`listFiles()` 保持 mock。
2. Workflow Runtime SSE/log stream 后端未实现，run log/node state stream 按任务要求继续使用 scripted mock。
3. Notify SSE/WS 仍依赖 userId path/query；浏览器 WebSocket 无法携带 Authorization header，后续需要后端 stream token/subprotocol 契约。
4. 本 Task 4 未修改 `frontend/src/router/index.ts`；该文件来自本分支早前提交，最终 `origin/main...HEAD` 路径检查会显示这一历史变更。

## FE-API-INTEGRATION-20260529 Task 5

时间：2026-05-29 +08:00
Agent ID：chyinan
Session ID：SESSION-20260529-1247-FE-API-INTEGRATION
分支：feature/FE-API-INTEGRATION-20260529-frontend-integration
状态：IN_PROGRESS

完成内容：
1. 新增 `frontend/src/api/modules/ai.ts`，接入 `GET /ai/status`、`GET /ai/provider/status`、`GET /ai/provider/policy`、`PUT /ai/provider/policy`、`POST /ai/provider/policy/recover/{provider}`、`GET /ai/provider/metrics`。
2. 新增 `frontend/src/api/mappers/aiMapper.ts`，把 Provider governance status / metrics / policy / logs 映射为现有 Models 页面类型；模型目录缺失字段使用 `runtime default`、`contract pending` 等明确占位。
3. 更新 `frontend/src/services/api/modelApi.ts`，Models facade 采用真实 AI Provider API 优先、同页快照缓存、仅网络/Gateway 不可用时 mock fallback，业务/鉴权错误不回退。
4. 更新 `frontend/src/stores/modelStore.ts`，保持 store 面向组件的字段与 `refreshMockProbe()` 名称，刷新时优先拉真实 Provider 数据，真实后端不可用时才执行原 mock probe。
5. 新增 `docs/frontend-backend-missing-apis.md`，按 P0/P1/P2 记录前端集成发现的后端缺口。

验证结果：
1. `cd frontend; npm run build`：通过。Vite chunk size warning 为既有打包体积提示。
2. `git diff --check`：通过，仅 Windows LF/CRLF 提示。
3. `git diff --check HEAD^..HEAD`：通过，无 whitespace error。
4. `git diff --name-only HEAD^..HEAD`：仅包含 Task 5 允许范围文件。

遗留问题：
1. AI Provider 后端仍缺少真实模型目录、价格/上下文窗口、Provider endpoint label、frontend-shaped log feed；已登记为 P1 backend backlog。
2. 需统一运行电脑补测 Gateway、ai-service 与真实 Provider health/metrics/logs 链路。

## FE-API-INTEGRATION-20260529 Final Handoff

时间：2026-05-29 16:11:00 +08:00
Agent ID：chyinan
Session ID：SESSION-20260529-1247-FE-API-INTEGRATION
分支：feature/FE-API-INTEGRATION-20260529-frontend-integration
状态：DONE

完成内容：
1. 完成企业级 Frontend API Integration Layer：Axios client、tokenManager、错误归一化、traceId、retry、OpenAPI/Orval SDK 生成配置。
2. 完成 Auth Token 生命周期、Pinia auth state、路由守卫和角色控制接入。
3. 完成 Workflow create/start、Workflow Builder DAG -> backend definition mapper、Runtime observability/events REST recovery、Node Catalog/metrics 接入。
4. 完成 File Service multipart upload、浏览器上传进度、server progress helper、download/delete API，以及 FileAsset mapper。
5. 完成 Notify fetch-SSE、heartbeat/idle recovery、bounded reconnect、可选 WebSocket fallback；WS fallback 默认关闭，等待后端安全 stream auth 契约。
6. 完成 AI Provider governance API 接入 Models facade/store，并保留缺失真实 model catalog 的保守占位。
7. 完成 API 401 场景的 refresh-token 队列与原请求重放，覆盖页面停留期间 access token 过期的恢复路径。
8. 完成 `docs/frontend-backend-missing-apis.md`，记录后端补齐 backlog。

验证结果：
1. `cd frontend; npm run build`：通过。Vite 仅输出既有 chunk size warning。
2. `git diff --check`：通过，无 whitespace error。
3. `git diff --name-only origin/main...HEAD`：通过，变更文件限定在本任务前端 API/Service/Realtime/Store/Types/Config/Router、frontend package/env、docs/AGENT 范围。
4. `cd frontend; npm run api:generate`：未通过。原因：本机未启动 Gateway/OpenAPI，Orval 无法解析 `http://localhost:8080/auth/v3/api-docs`、`/workflows/v3/api-docs`、`/ai/v3/api-docs`、`/files/v3/api-docs`、`/notify/v3/api-docs`。

提交记录：
1. `3713838 docs(agent): claim FE-API-INTEGRATION-20260529`
2. `f736edc docs(frontend): plan api integration`
3. `4ec7d0e feat(frontend): add api client foundation`
4. `ae7558e feat(frontend): integrate auth token lifecycle`
5. `1d4bbed feat(frontend): integrate workflow runtime APIs`
6. `c550695 feat(frontend): integrate file upload and notify realtime`
7. `2ff6a1d fix(frontend): tighten notify stream parsing and upload fallback`
8. `8609fb1 fix(frontend): isolate notify realtime state`
9. `b5a3c47 fix(frontend): gate notify websocket fallback`
10. `e7e0b03 feat(frontend): integrate ai provider models`
11. `fda05bb fix(frontend): avoid precise ai model fallbacks`
12. `a31be4b fix(frontend): tighten ai provider snapshot mapping`
13. `336d7a9 fix(frontend): preserve real model snapshot on fallback`
14. `4534993 fix(frontend): refresh session on unauthorized api calls`

统一运行电脑验证：未运行。

遗留问题：
1. 需统一运行电脑启动 Gateway 与 auth-service、workflow-service、file-service、notify-service、ai-service 后补测 Auth、Workflow create/start、Runtime recovery、File upload、Notify SSE、AI Provider Models。
2. `npm run api:generate` 需在 Gateway OpenAPI 聚合可访问后重跑。
3. 后端缺口详见 `docs/frontend-backend-missing-apis.md`，建议优先处理 P0 `/workflow/**` Gateway route、Workflow list/detail/update、Run list/detail/log。

文件锁：RELEASED。

## BE-STREAM-AUTH-20260529 Claim

时间：2026-05-29 18:20:00 +08:00
Agent ID：chyinan
Session ID：SESSION-20260529-1820-BE-STREAM-AUTH
分支：feature/BE-STREAM-AUTH-20260529-stream-auth
状态：IN_PROGRESS

任务目标：

1. 新增 `POST /notify/stream-token`，由已鉴权 REST 请求签发 1 分钟短期 stream token。
2. `/notify/ws` 通过 `streamToken` 或 `token` query 参数校验身份，不再信任 `userId` query。
3. Gateway 对 `/notify/ws` 放行 Bearer 校验，由 notify-service 做 stream token 校验。

文件锁：

1. backend/notify-service/src/main/java/com/aetherflow/notify/controller/NotifyController.java
2. backend/notify-service/src/main/java/com/aetherflow/notify/config/WebSocketConfig.java
3. backend/notify-service/src/main/java/com/aetherflow/notify/config/NotifySecurityConfig.java
4. backend/notify-service/src/main/java/com/aetherflow/notify/service/NotificationWebSocketHandler.java
5. backend/notify-service/src/main/java/com/aetherflow/notify/service/StreamTokenService.java
6. backend/notify-service/src/main/java/com/aetherflow/notify/service/StreamTokenHandshakeInterceptor.java
7. backend/notify-service/src/main/java/com/aetherflow/notify/dto/StreamTokenResponse.java
8. backend/notify-service/src/test/java/com/aetherflow/notify/controller/NotifyControllerTest.java
9. backend/notify-service/src/test/java/com/aetherflow/notify/service/StreamTokenServiceTest.java
10. backend/notify-service/src/test/java/com/aetherflow/notify/service/StreamTokenHandshakeInterceptorTest.java
11. backend/notify-service/src/test/java/com/aetherflow/notify/openapi/NotifyOpenApiContractTest.java
12. backend/gateway-service/src/main/resources/application.yml
13. backend/gateway-service/src/main/java/com/aetherflow/gateway/config/GatewaySecurityProperties.java
14. backend/gateway-service/src/test/java/com/aetherflow/gateway/filter/JwtAuthenticationFilterTest.java
15. backend/gateway-service/src/test/java/com/aetherflow/gateway/GatewayRouteConfigurationTest.java
16. docs/agent/tasks/BE-STREAM-AUTH-20260529.md
17. docs/agent/logs/2026-05-29.md
18. AGENT.md

契约登记：

1. `POST /notify/stream-token`：APPROVED。
2. `/notify/ws?streamToken=...` 或 `/notify/ws?token=...`：APPROVED。
3. Gateway `/notify/ws` permit-all，notify-service stream token 校验：APPROVED。

验证计划：

1. git diff --check
2. git diff --name-only main...HEAD
3. JAVA_HOME=C:\Program Files\Microsoft\jdk-17.0.19.10-hotspot; mvn -pl backend/notify-service,backend/gateway-service -am -Dtest=NotifyControllerTest,StreamTokenServiceTest,StreamTokenHandshakeInterceptorTest,NotifyOpenApiContractTest,JwtAuthenticationFilterTest,GatewayRouteConfigurationTest -Dsurefire.failIfNoSpecifiedTests=false test
4. JAVA_HOME=C:\Program Files\Microsoft\jdk-17.0.19.10-hotspot; mvn -pl backend/notify-service,backend/gateway-service -am test

## BE-STREAM-AUTH-20260529 Completion

时间：2026-05-29 18:31:00 +08:00
Agent ID：chyinan
Session ID：SESSION-20260529-1820-BE-STREAM-AUTH
分支：feature/BE-STREAM-AUTH-20260529-stream-auth
状态：REVIEW

完成内容：

1. `POST /notify/stream-token` 已新增，返回 `StreamTokenResponse`，token 有效期 60 秒。
2. `/notify/ws` 已接入 `StreamTokenHandshakeInterceptor`，支持 `streamToken` 或 `token` query 参数。
3. `NotificationWebSocketHandler` 改为读取已校验 handshake attributes，不再从 URL userId 建立身份。
4. Gateway `permit-all` 增加 `/notify/ws`，其余 `/notify/**` 仍需 Bearer。
5. 未修改 frontend、common、DB、Redis、MQ、Nacos、notify-service application.yml。

验证记录：

1. TDD 红灯：目标测试首次执行未通过，Gateway `/notify/ws` 未放行，Notify token/interceptor 类缺失，符合预期。
2. 目标测试通过：`JAVA_HOME=C:\Program Files\Microsoft\jdk-17.0.19.10-hotspot; mvn -pl backend/notify-service,backend/gateway-service -am -Dtest=NotifyControllerTest,StreamTokenServiceTest,StreamTokenHandshakeInterceptorTest,NotifyOpenApiContractTest,JwtAuthenticationFilterTest,GatewayRouteConfigurationTest -Dsurefire.failIfNoSpecifiedTests=false test`。
   - 证据：gateway-service 12 tests；notify-service 6 tests；BUILD SUCCESS。
3. 全量测试通过：`JAVA_HOME=C:\Program Files\Microsoft\jdk-17.0.19.10-hotspot; mvn -pl backend/notify-service,backend/gateway-service -am test`。
   - 证据：common 8 tests；gateway-service 19 tests；notify-service 6 tests；BUILD SUCCESS。
4. 静态检查通过：`git diff --check` 无 whitespace error，仅 Windows LF/CRLF 提示。
5. 修改范围检查通过：`git diff --name-only main...HEAD` 限定在任务文件锁范围。

提交：

1. 8001e03 docs(agent): claim BE-STREAM-AUTH-20260529
2. 8275135 feat(notify): add stream token websocket auth

统一运行电脑验证：未运行。

遗留问题：

1. 需统一运行电脑补测真实 Gateway WebSocket upgrade、notify-service handshake、JWT/stream token 链路。
2. 前端 WS fallback 未在本任务启用，需后续前端任务接入 `POST /notify/stream-token` 后打开。

文件锁：RELEASED。

## FE-API-INTEGRATION-20260529 Main Merge

时间：2026-05-29 16:50:00 +08:00
Agent ID：chyinan
Session ID：SESSION-20260529-1247-FE-API-INTEGRATION
分支：main
状态：DONE

记录：
1. 已按用户指令将 `feature/FE-API-INTEGRATION-20260529-frontend-integration` 合入 `main`。
2. 合入前 `main` 先同步远端最新提交，远端包含若干页面文字/导航小改动；合并未发生冲突。
3. main 合入提交：`92ced64 merge: frontend api integration layer`。
4. main 上 `cd frontend; npm run build` 通过，Vite 仅输出既有 chunk size warning。
5. main 上 `git diff --check HEAD^1..HEAD` 通过，无 whitespace error。
6. 文件锁保持 RELEASED。

统一运行电脑验证：未运行。

遗留问题：
1. 需统一运行电脑补测真实 Gateway OpenAPI、Auth、Workflow create/start、Runtime recovery、File upload、Notify SSE、AI Provider。
2. `npm run api:generate` 需在 Gateway/OpenAPI 可访问后重跑。
3. 后端缺口详见 `docs/frontend-backend-missing-apis.md`。

## BE-SETTINGS-ADMIN-20260529 Claim

时间：2026-05-29 19:25 +08:00
Agent ID：chyinan
Session ID：SESSION-20260529-1925-BE-SETTINGS-ADMIN
分支：feature/BE-SETTINGS-ADMIN-20260529-settings-admin
状态：IN_PROGRESS

记录：

1. 已读取 AGENT.md、docs/frontend-backend-missing-apis.md、FE-API-INTEGRATION-20260529 任务文档和前端 Settings mock 类型。
2. 本任务补齐 Settings/admin API 最小后端闭环，归属 auth-service。
3. 允许新增 `/settings/**` REST API、auth-service 内部 DTO、`af_settings_profile`/`af_settings_member`/`af_settings_billing`/`af_settings_audit_event` DB 表和 Gateway 路由。
4. 禁止修改前端、auth 现有登录注册/token 代码、common、Redis、MQ、Nacos 和 Maven 依赖。
5. 已登记任务文档、文件锁和契约变更；claim push 成功前不修改业务代码。

验证计划：

1. git diff --check
2. git diff --name-only main...HEAD
3. JAVA_HOME=C:\Program Files\Microsoft\jdk-17.0.19.10-hotspot; mvn -pl backend/auth-service -am -Dtest=SettingsControllerTest,SettingsServiceImplTest,SettingsSchemaTest -Dsurefire.failIfNoSpecifiedTests=false test
4. JAVA_HOME=C:\Program Files\Microsoft\jdk-17.0.19.10-hotspot; mvn -pl backend/gateway-service -am -Dtest=GatewayRouteConfigurationTest -Dsurefire.failIfNoSpecifiedTests=false test
5. JAVA_HOME=C:\Program Files\Microsoft\jdk-17.0.19.10-hotspot; mvn -pl backend/auth-service,backend/gateway-service -am test

## BE-SETTINGS-ADMIN-20260529 Handoff

时间：2026-05-29 19:34 +08:00
Agent ID：chyinan
Session ID：SESSION-20260529-1925-BE-SETTINGS-ADMIN
分支：feature/BE-SETTINGS-ADMIN-20260529-settings-admin
状态：REVIEW

完成内容：

1. 新增 auth-service Settings/admin API 最小后端闭环。
2. 新增 Settings DTO、entity、mapper、service、controller 和 SQL schema。
3. profile/member 写操作记录 audit event；member delete 为软删除。
4. 新增 Gateway `/settings/** -> auth-service` route 和 auth-api Sentinel pattern。
5. 补齐 controller/service/schema/gateway route contract 测试。

验证结果：

1. TDD Red：实现前 Settings 目标测试编译失败，Gateway route 测试确认 `/settings/**` 缺失。
2. `git diff --check`：通过，无 whitespace error，仅 Windows LF/CRLF 提示。
3. `mvn -pl backend/auth-service -am -Dtest=SettingsControllerTest,SettingsServiceImplTest,SettingsSchemaTest -Dsurefire.failIfNoSpecifiedTests=false test`：通过，10 tests。
4. `mvn -pl backend/gateway-service -am -Dtest=GatewayRouteConfigurationTest -Dsurefire.failIfNoSpecifiedTests=false test`：通过，5 tests。
5. `mvn -pl backend/auth-service,backend/gateway-service -am test`：通过，common 8 tests；gateway-service 17 tests；auth-service 40 tests。

提交：

1. `ff3a67e docs(agent): claim BE-SETTINGS-ADMIN-20260529`
2. `053f61c feat(auth): add settings admin APIs`

合入 main：未合入。

统一运行电脑验证：未运行。

遗留问题：

1. 需统一运行电脑应用 `backend/auth-service/src/main/resources/db/settings-admin.sql` 或 docker init SQL 后补测真实 MySQL。
2. 需补测 Gateway `/settings/**`、auth-service 启动和 Settings 页面 API 链路。
3. 真实支付渠道、发票、套餐变更不在本任务范围。

文件锁：RELEASED。

## WORKFLOW-EMBEDDING-NODE-20260529 Claim

时间：2026-05-29 10:01:00 +08:00
Agent ID：chyinan
Session ID：SESSION-20260529-1001-CODEX-WORKFLOW-EMBEDDING
分支：feature/WORKFLOW-EMBEDDING-NODE-20260529-embedding-node
状态：IN_PROGRESS

记录：
1. 已读取 AGENT.md 和 docs/COMMON_CONTRACTS.md。
2. 已确认任务边界：只实现 workflow-service Embedding NodeExecutor、Embedding Provider System、Text Splitter、Mock Vector Store、Metrics API、Swagger/Catalog 和配置。
3. 已创建隔离 worktree：`C:\Users\chyinan\.config\superpowers\worktrees\AetherFlow\WORKFLOW-EMBEDDING-NODE-20260529-embedding-node`。
4. 当前分支从 origin/main 创建：`feature/WORKFLOW-EMBEDDING-NODE-20260529-embedding-node`。
5. 已检查 AGENT.md 文件锁表，目标范围未发现 ACTIVE 冲突。
6. 当前进行 docs-only claim；claim push 成功前不修改业务代码。

Runtime 边界：
1. 不修改 workflow-runtime-api。
2. 不修改 RuntimeState、WorkflowContext、NodeExecutor、NodeResult。
3. 不修改 Runtime Core、DAG Engine、Workflow 生命周期、Retry、Recovery、Lock、Event Stream。
4. Embedding timeout / provider failure 抛异常，由 Runtime Retry 控制。

Node 边界：
1. EmbeddingNodeExecutor 只读取 WorkflowContext 和节点 config，返回 NodeResult.variables。
2. Provider 通过 EmbeddingProviderRegistry 选择，Executor 不写死 Ollama。
3. Text Splitter 负责 chunkSize / overlap，默认 512 / 128。
4. Mock Vector Store 只用于演示知识库加工，不新增 DB、Redis 或外部向量库契约。

不会修改：
1. backend/workflow-runtime-api/**
2. backend/workflow-service/src/main/java/com/aetherflow/workflow/runtime/**
3. backend/ai-service/**
4. backend/file-service/**
5. backend/common/**
6. backend/gateway-service/**
7. DB / MQ / Redis / Gateway 契约。

文件锁申请：
1. backend/workflow-service/src/main/java/com/aetherflow/workflow/embedding/**
2. backend/workflow-service/src/main/java/com/aetherflow/workflow/node/executor/EmbeddingNodeExecutor.java
3. backend/workflow-service/src/main/java/com/aetherflow/workflow/node/WorkflowNodeTypes.java
4. backend/workflow-service/src/main/java/com/aetherflow/workflow/node/catalog/WorkflowNodeCatalogService.java
5. backend/workflow-service/src/test/java/com/aetherflow/workflow/embedding/**
6. backend/workflow-service/src/test/java/com/aetherflow/workflow/node/executor/EmbeddingNodeExecutorTest.java
7. backend/workflow-service/src/test/java/com/aetherflow/workflow/node/controller/WorkflowNodeCatalogControllerTest.java
8. backend/workflow-service/src/test/java/com/aetherflow/workflow/openapi/WorkflowOpenApiContractTest.java
9. backend/workflow-service/pom.xml
10. backend/workflow-service/src/main/resources/application.yml
11. docs/agent/tasks/WORKFLOW-EMBEDDING-NODE-20260529.md
12. docs/agent/logs/2026-05-29.md
13. AGENT.md

环境检测：
1. git：git version 2.53.0.windows.3。
2. java：OpenJDK 17.0.19 Microsoft。
3. maven：Apache Maven 3.9.9，Java version 17.0.19。
4. node：v24.15.0。
5. npm：11.12.1。
6. OS：Microsoft Windows NT 10.0.26200.0。
7. 检测时间：2026-05-29 10:01 +08:00。
8. 不能执行的命令：无。
9. 是否需要统一运行电脑补测：是，涉及真实 Ollama、workflow-service 启动、Nacos、MySQL、Redis 链路。

验证计划：
1. git diff --check。
2. JAVA_HOME=C:\Program Files\Microsoft\jdk-17.0.19.10-hotspot; mvn -pl backend/workflow-service -am -Dtest=EmbeddingNodeConfigTest,SimpleTextSplitterTest,EmbeddingProviderRegistryTest,EmbeddingNodeExecutorTest,EmbeddingMetricsControllerTest,WorkflowNodeCatalogControllerTest,WorkflowOpenApiContractTest -Dsurefire.failIfNoSpecifiedTests=false test。
3. JAVA_HOME=C:\Program Files\Microsoft\jdk-17.0.19.10-hotspot; mvn -pl backend/common,backend/workflow-service -am test。
4. git diff --name-only main...HEAD。

## WORKFLOW-EMBEDDING-NODE-20260529 Implementation

时间：2026-05-29 10:30:00 +08:00
Agent ID：chyinan
Session ID：SESSION-20260529-1001-CODEX-WORKFLOW-EMBEDDING
分支：feature/WORKFLOW-EMBEDDING-NODE-20260529-embedding-node
状态：REVIEW

完成内容：
1. 新增 EmbeddingProvider 抽象、EmbeddingProviderRegistry 和 OllamaEmbeddingProvider。
2. 新增 EmbeddingRequest、EmbeddingResult、EmbeddingNodeConfig、TextChunk、TextSplitter、SimpleTextSplitter。
3. 新增 MockVectorStore 和 MockVectorRecord，支撑 Upload -> Split -> Embedding -> Vector Store 演示链路。
4. 新增 EmbeddingMetrics、EmbeddingMetricsSnapshot 和 GET /workflow/embedding/metrics，补齐 Swagger summary、description、example。
5. 新增 EmbeddingNodeExecutor，读取 WorkflowContext 文本，按 chunkSize / overlap 切分，调用 provider，写入 NodeResult.variables。
6. 更新 WorkflowNodeTypes 和 WorkflowNodeCatalogService，Catalog 增加 EMBEDDING 节点配置 schema 和输出变量。
7. 更新 workflow-service pom.xml、application.yml，接入 Spring AI Ollama 和 aetherflow.workflow.embedding 配置。
8. 补齐配置、切分、provider registry、Ollama provider、Mock Vector Store、NodeExecutor、Metrics API、Catalog、OpenAPI 合约测试。

Runtime 边界复核：
1. 未修改 backend/workflow-runtime-api/**。
2. 未修改 RuntimeState、WorkflowContext、NodeExecutor、NodeResult 协议结构。
3. 未修改 backend/workflow-service/src/main/java/com/aetherflow/workflow/runtime/**。
4. 未修改 DAG Runtime Engine、Workflow 生命周期、Runtime Retry。
5. Embedding timeout / provider failure 仅抛 BusinessException，由 Runtime 现有 RetryPolicy 接管。

验证结果：
1. git diff --check：通过，无 whitespace error，仅 Windows LF/CRLF 提示。
2. git diff --cached --check：通过。
3. 目标测试通过：JAVA_HOME=C:\Program Files\Microsoft\jdk-17.0.19.10-hotspot; mvn -pl backend/workflow-service -am -Dtest=EmbeddingNodeConfigTest,SimpleTextSplitterTest,EmbeddingProviderRegistryTest,OllamaEmbeddingProviderTest,MockVectorStoreTest,EmbeddingNodeExecutorTest,EmbeddingMetricsControllerTest,WorkflowNodeCatalogControllerTest,WorkflowOpenApiContractTest -Dsurefire.failIfNoSpecifiedTests=false test；workflow-service 16 tests；BUILD SUCCESS。
4. 相关模块测试通过：JAVA_HOME=C:\Program Files\Microsoft\jdk-17.0.19.10-hotspot; mvn -pl backend/common,backend/workflow-service -am test；common 8 tests；workflow-runtime-api 10 tests；workflow-service 84 tests；BUILD SUCCESS。

提交：
1. 093f1c7 docs(agent): claim WORKFLOW-EMBEDDING-NODE-20260529
2. cdb99ec feat(workflow): add embedding node system

遗留问题：
1. 需统一运行电脑补测 workflow-service 启动、真实 Ollama base-url、nomic-embed-text / bge-m3 模型、Nacos/MySQL/Redis 链路。
2. Mock Vector Store 仅用于演示知识库加工，后续真实 Milvus / PgVector 接入需要单独任务和契约登记。
3. 当前向量结果写入 WorkflowContext.variables 用于节点链路衔接；生产向量库接入后应减少 Runtime snapshot 中的大向量驻留。

文件锁：
1. WORKFLOW-EMBEDDING-NODE-20260529 文件锁已 RELEASED。

## WORKFLOW-EMBEDDING-NODE-20260529 Main Merge

时间：2026-05-29 10:48:00 +08:00
Agent ID：chyinan
Session ID：SESSION-20260529-1001-CODEX-WORKFLOW-EMBEDDING
分支：main
状态：DONE

记录：
1. 已按负责人指令将 feature/WORKFLOW-EMBEDDING-NODE-20260529-embedding-node 合入 main。
2. 主线合入提交：c2e2c3b merge: workflow embedding node system。
3. main 合入检查通过：git diff --check HEAD^1..HEAD，无 whitespace error。
4. main 相关模块测试通过：JAVA_HOME=C:\Program Files\Microsoft\jdk-17.0.19.10-hotspot; mvn -pl backend/common,backend/workflow-service -am test。
5. 测试证据：common 8 tests；workflow-runtime-api 10 tests；workflow-service 84 tests；BUILD SUCCESS。
6. 文件锁保持 RELEASED。

统一运行电脑补测：
1. 未运行。
2. 仍需补测 workflow-service 启动、真实 Ollama base-url、nomic-embed-text / bge-m3 模型、Nacos/MySQL/Redis 链路。

## WORKFLOW-OCR-NODE-20260529

时间：2026-05-29 08:45 +08:00

Agent：chyinan

分支：feature/WORKFLOW-OCR-NODE-20260529-ocr-node

状态：IN_PROGRESS

### 开工记录

已读取：

1. AGENT.md
2. docs/COMMON_CONTRACTS.md
3. superpowers:using-superpowers
4. superpowers:brainstorming
5. superpowers:writing-plans
6. superpowers:test-driven-development
7. superpowers:verification-before-completion

### 环境检测

- git：git version 2.53.0.windows.3
- java：openjdk version "17.0.19" 2026-04-21 LTS
- maven：Apache Maven 3.9.9，默认 Java version 11.0.31
- node：v24.15.0
- npm：11.12.1
- 操作系统：Microsoft Windows 11 专业工作站版
- 检测时间：2026-05-29 08:45 +08:00
- 不能执行的命令：无
- 是否需要统一运行电脑补测：是，需补测真实 Tesseract、MinIO、Nacos、file-service 内部下载链路

### Git 同步

- git status --short --branch：main...origin/main，无本地业务改动
- git switch main：已在 main
- git pull origin main：Already up to date
- 创建分支：feature/WORKFLOW-OCR-NODE-20260529-ocr-node

### Runtime 边界

1. 不修改 workflow-runtime-api。
2. 不修改 RuntimeState。
3. 不修改 DAG Runtime Engine。
4. 不修改 Workflow 生命周期。
5. OCR Retry 交给 Runtime，节点内部不做死循环。

### Node 边界

1. OCR 作为新 NodeExecutor 接入现有 BaseNodeExecutor。
2. OCRNodeExecutor 通过 file-service 内部下载接口获取文件。
3. OCR 实现通过 OCRProvider 抽象注入。
4. OCR 输出写入 WorkflowContext variables。

### Claim 内容

1. 登记任务看板。
2. 登记文件锁。
3. 登记契约变更。
4. 写入任务文档和编码计划。

### 验证计划

1. git diff --check
2. JAVA_HOME=C:\Program Files\Microsoft\jdk-17.0.19.10-hotspot; mvn -pl backend/common,backend/file-service,backend/workflow-service -am test

### 完成记录

时间：2026-05-29 09:09 +08:00

完成内容：

1. file-service 新增内部下载接口 GET /internal/files/{fileId}/download。
2. workflow-service 新增 OCRProvider 抽象、TesseractOCRProvider、MockOCRProvider、OCRNodeExecutor。
3. OCRNodeExecutor 通过 file-service Feign 获取文件二进制，写入 ocrText、ocrLanguage、ocrConfidence、ocrPageCount。
4. 新增 /workflow/ocr/metrics。
5. 新增 OCR 节点 catalog 和 Swagger/OpenAPI 覆盖。
6. 新增 mock=true 演示模式。
7. PDF 文本层优先 PDFBox 提取，扫描 PDF 回退 Tesseract。

验证记录：

1. git diff --check
   - 结果：通过
   - 证据：无 whitespace error，仅 Windows LF/CRLF 提示
2. JAVA_HOME=C:\Program Files\Microsoft\jdk-17.0.19.10-hotspot; mvn -pl backend/workflow-service -am -Dtest=OCRNodeConfigTest,OCRProviderRegistryTest,TesseractOCRProviderTest,OCRNodeExecutorTest,OCRMetricsControllerTest,WorkflowNodeCatalogControllerTest,WorkflowOpenApiContractTest -Dsurefire.failIfNoSpecifiedTests=false test
   - 结果：通过
   - 证据：OCR 相关 12 tests；BUILD SUCCESS
3. JAVA_HOME=C:\Program Files\Microsoft\jdk-17.0.19.10-hotspot; mvn -pl backend/common,backend/file-service,backend/workflow-service -am test
   - 结果：通过
   - 证据：common 8 tests；workflow-runtime-api 10 tests；workflow-service 80 tests；file-service 23 tests；BUILD SUCCESS

提交：

1. 3926346 docs(agent): claim WORKFLOW-OCR-NODE-20260529
2. e95dd4b feat(workflow): add ocr node system

状态：REVIEW

统一运行电脑验证：未运行

遗留问题：

1. 需统一运行电脑补测真实 Tesseract native binary / tessdata。
2. 需补测真实 MinIO 文件下载和 Upload -> OCR -> Summary -> Export DAG 链路。

文件锁：RELEASED

## BE-BACKEND-GAPS-MAIN-MERGE-20260529 Main Merge

时间：2026-05-29 20:09 +08:00
Agent ID：chyinan
分支：main
状态：DONE

完成内容：

1. 已按负责人指令将 12 个后端缺口 feature 分支全部合入 main。
2. P0 合入：Gateway `/workflow/**` route；Workflow definitions list/detail/update/delete；Workflow run list/detail/log。
3. P1 合入：Runtime SSE；Stream auth；File list/query；Chunk upload；AI Provider catalog/log/policy；Project/workspace。
4. P2 合入：Knowledge dataset/document/chunk；Settings/member/billing/audit；Copilot chat。
5. 合并冲突处理保留所有业务契约：Gateway route/test 同时覆盖 workflow、projects、knowledge、settings、copilot、notify WS；FileController 同时保留 list/query 与 chunk upload；docker MySQL init SQL 保留 project、knowledge、settings、copilot 表。

合入提交：

1. 23a2fee merge: backend gateway workflow route
2. 9d81eaa merge: backend workflow definition crud
3. 12dcd4e merge: backend workflow run query
4. ed506e3 merge: backend workflow runtime sse
5. 2a9638e merge: backend stream auth
6. a0eec0b merge: backend file list query
7. 6c7a2b6 merge: backend file chunk upload
8. 760f6ee merge: backend ai provider catalog
9. fe94377 merge: backend project workspace
10. b8929e7 merge: backend knowledge dataset
11. cadb533 merge: backend settings admin
12. 39937cc merge: backend copilot chat

验证记录：

1. `rg -n "^(<<<<<<<|=======|>>>>>>>)" AGENT.md docs/agent/logs/2026-05-29.md backend docker`
   - 结果：通过
   - 证据：无冲突标记输出
2. `git diff --check`
   - 结果：通过
   - 证据：无 whitespace error
3. `JAVA_HOME=C:\Program Files\Microsoft\jdk-17.0.19.10-hotspot; mvn -pl backend/auth-service,backend/workflow-service,backend/file-service,backend/ai-service,backend/notify-service,backend/gateway-service -am test`
   - 结果：通过
   - 证据：common 9 tests；workflow-runtime-api 10 tests；gateway-service 24 tests；auth-service 40 tests；workflow-service 135 tests；ai-service 32 tests；file-service 35 tests；notify-service 6 tests；BUILD SUCCESS

合入 main：已合入

统一运行电脑验证：未运行

遗留问题：

1. 需统一运行电脑应用新增 SQL：project/workspace、knowledge、settings、copilot。
2. 需补测真实 Gateway/Nacos/MySQL/Redis/MinIO/WS/SSE/前后端链路。
3. Copilot 当前为 deterministic backend heuristic，未接外部 LLM 或流式回复。

文件锁：RELEASED

## WORKFLOW-OCR-NODE-20260529 Main Merge

时间：2026-05-29 11:02:00 +08:00
Agent ID：chyinan
Session ID：SESSION-20260529-0845-WORKFLOW-OCR
分支：main
状态：DONE

记录：
1. 已按负责人指令将 feature/WORKFLOW-OCR-NODE-20260529-ocr-node 合入 main。
2. 主线合入提交：d87922a merge: workflow ocr node system。
3. main 合入检查通过：git diff --check HEAD^1..HEAD，无 whitespace error。
4. main 相关模块测试通过：JAVA_HOME=C:\Program Files\Microsoft\jdk-17.0.19.10-hotspot; mvn -pl backend/common,backend/file-service,backend/workflow-service -am test。
5. 测试证据：common 8 tests；workflow-runtime-api 10 tests；workflow-service 94 tests；file-service 23 tests；BUILD SUCCESS。
6. 文件锁保持 RELEASED。

统一运行电脑补测：
1. 未运行。
2. 仍需补测 workflow-service/file-service 启动、真实 MinIO 文件下载、Tesseract native binary 与 tessdata、OCR 节点 DAG 链路。

## BE-AI-PROVIDER-CATALOG-20260529 Claim

时间：2026-05-29 18:07:00 +08:00
Agent ID：chyinan
Session ID：SESSION-20260529-1807-BE-AI-PROVIDER-CATALOG
分支：feature/BE-AI-PROVIDER-CATALOG-20260529-provider-catalog
状态：IN_PROGRESS

任务目标：

1. 新增 `GET /ai/provider/catalog`，返回前端模型治理页需要的 provider endpoint label、默认模型、context window、pricing metadata、capabilities。
2. 新增 `GET /ai/provider/logs?limit=`，把现有 AIInferenceLog 映射为 frontend-shaped runtime log feed。
3. 扩展 `ProviderRoutingPolicy.requestTimeout`，默认来自 `aetherflow.ai.provider-timeout`，旧 Redis policy JSON 缺失字段时保持兼容。

文件锁：

1. backend/ai-service/src/main/java/com/aetherflow/ai/controller/AiProviderController.java
2. backend/ai-service/src/main/java/com/aetherflow/ai/provider/ProviderRoutingPolicy.java
3. backend/ai-service/src/main/java/com/aetherflow/ai/provider/ProviderRoutingPolicyService.java
4. backend/ai-service/src/main/java/com/aetherflow/ai/provider/ProviderCatalogService.java
5. backend/ai-service/src/main/java/com/aetherflow/ai/provider/ProviderCatalogResponse.java
6. backend/ai-service/src/main/java/com/aetherflow/ai/provider/ProviderRuntimeLogResponse.java
7. backend/ai-service/src/test/java/com/aetherflow/ai/controller/AiProviderControllerTest.java
8. backend/ai-service/src/test/java/com/aetherflow/ai/provider/ProviderCatalogServiceTest.java
9. backend/ai-service/src/test/java/com/aetherflow/ai/provider/ProviderRoutingPolicyTest.java
10. backend/ai-service/src/test/java/com/aetherflow/ai/openapi/AiOpenApiContractTest.java
11. docs/agent/tasks/BE-AI-PROVIDER-CATALOG-20260529.md
12. docs/agent/logs/2026-05-29.md
13. AGENT.md

契约登记：

1. `GET /ai/provider/catalog`：APPROVED。
2. `GET /ai/provider/logs?limit=`：APPROVED。
3. `ProviderRoutingPolicy.requestTimeout`：APPROVED。
4. ProviderRoutingPolicy Redis JSON 兼容性新增 `requestTimeout` 字段：APPROVED，不新增 Redis Key。

验证计划：

1. git diff --check
2. git diff --name-only main...HEAD
3. JAVA_HOME=C:\Program Files\Microsoft\jdk-17.0.19.10-hotspot; mvn -pl backend/ai-service -am -Dtest=AiProviderControllerTest,ProviderCatalogServiceTest,ProviderRoutingPolicyTest,AiOpenApiContractTest -Dsurefire.failIfNoSpecifiedTests=false test
4. JAVA_HOME=C:\Program Files\Microsoft\jdk-17.0.19.10-hotspot; mvn -pl backend/ai-service -am test

## BE-AI-PROVIDER-CATALOG-20260529 Completion

时间：2026-05-29 18:15:00 +08:00
Agent ID：chyinan
Session ID：SESSION-20260529-1807-BE-AI-PROVIDER-CATALOG
分支：feature/BE-AI-PROVIDER-CATALOG-20260529-provider-catalog
状态：REVIEW

完成内容：

1. 新增 `GET /ai/provider/catalog`，返回 provider cards、model catalog rows、endpoint label、context window、pricing metadata、capabilities。
2. 新增 `GET /ai/provider/logs?limit=`，复用 AIInferenceLog 并映射为前端 runtime log feed。
3. `ProviderRoutingPolicy` 新增向后兼容字段 `requestTimeout`，默认来自 `aetherflow.ai.provider-timeout`。
4. 未修改 frontend、Gateway、common、DB、MQ、Nacos、application.yml。

验证记录：

1. TDD 红灯：目标测试首次执行未通过，缺少 ProviderCatalogResponse / ProviderCatalogService / ProviderRuntimeLogResponse，符合预期。
2. 目标测试通过：`JAVA_HOME=C:\Program Files\Microsoft\jdk-17.0.19.10-hotspot; mvn -pl backend/ai-service -am -Dtest=AiProviderControllerTest,ProviderCatalogServiceTest,ProviderRoutingPolicyTest,AiOpenApiContractTest -Dsurefire.failIfNoSpecifiedTests=false test`。
   - 证据：9 tests，BUILD SUCCESS。
3. 全量测试通过：`JAVA_HOME=C:\Program Files\Microsoft\jdk-17.0.19.10-hotspot; mvn -pl backend/ai-service -am test`。
   - 证据：common 8 tests；ai-service 25 tests；BUILD SUCCESS。
4. 静态检查通过：`git diff --check` 无 whitespace error，仅 Windows LF/CRLF 提示。
5. 修改范围检查通过：`git diff --name-only main...HEAD` 限定在任务文件锁范围。

提交：

1. d04fba0 docs(agent): claim BE-AI-PROVIDER-CATALOG-20260529
2. f9ab5d7 feat(ai): add provider catalog APIs

统一运行电脑验证：未运行。

遗留问题：

1. 需统一运行电脑补测 Gateway `/ai/provider/**`、ai-service、Redis、Python AI runtime 真实链路。
2. 真实 OpenAI/Ollama 模型可用性和价格应由后续配置化元数据源补齐，本任务只提供后端结构和静态默认。

文件锁：RELEASED。
